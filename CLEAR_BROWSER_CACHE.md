# 🔄 浏览器缓存清除指南

## ⚠️ 为什么需要清除缓存？

当我们修复代码后，**浏览器可能仍然显示旧版本的页面**，这是因为：

1. **浏览器缓存**: 浏览器会缓存HTML、CSS、JavaScript文件以加快加载速度
2. **CDN缓存**: 如果使用了CDN，CDN也会缓存静态文件
3. **Service Worker**: 某些网站使用Service Worker进行离线缓存

## ✅ 症状判断

如果您遇到以下情况，说明需要清除缓存：

- ❌ "前12小时"和"后12小时"按钮不显示
- ❌ 页面标题没有显示 `[v2.0-12小时分页]`
- ❌ 点击"后12小时"没有反应
- ❌ 时间显示的是UTC而非北京时间

## 🔧 清除缓存方法

### 方法1: 强制刷新（推荐）✅

**Windows / Linux**:
```
Ctrl + Shift + R
或
Ctrl + F5
```

**Mac**:
```
Cmd + Shift + R
或
Cmd + Option + R
```

**Chrome / Edge**: 在页面上按 `F12` 打开开发者工具，然后在刷新按钮上**长按**，选择"清空缓存并硬性重新加载"

### 方法2: 清除特定网站缓存

#### Chrome / Edge:
1. 点击地址栏前的 🔒 或 🌐 图标
2. 点击"网站设置"或"Cookie和网站数据"
3. 点击"清除数据"
4. 刷新页面 (F5)

#### Firefox:
1. 点击地址栏前的 🔒 图标
2. 点击"清除Cookie和网站数据"
3. 点击"清除"
4. 刷新页面 (F5)

#### Safari:
1. Safari菜单 → 偏好设置 → 隐私
2. 点击"管理网站数据"
3. 找到并删除对应的网站
4. 刷新页面 (Cmd + R)

### 方法3: 开发者模式（最彻底）✅

1. **打开开发者工具**: 按 `F12` 键
2. **打开网络面板**: 点击"Network"或"网络"标签
3. **禁用缓存**: 勾选"Disable cache"或"禁用缓存"复选框
4. **刷新页面**: 按 `F5` 或 `Ctrl+R`
5. **关闭开发者工具**: 再次按 `F12`

### 方法4: 使用隐私/无痕模式

**Chrome / Edge**:
```
Ctrl + Shift + N  (Windows/Linux)
Cmd + Shift + N   (Mac)
```

**Firefox**:
```
Ctrl + Shift + P  (Windows/Linux)
Cmd + Shift + P   (Mac)
```

**Safari**:
```
Cmd + Shift + N
```

在无痕模式下访问: https://5000-iypypqmz2wvn9dmtq7ewn-583b4d74.sandbox.novita.ai/support-resistance

### 方法5: 添加URL参数破坏缓存

在URL后面添加随机参数：
```
https://5000-xxx.sandbox.novita.ai/support-resistance?v=20251213
```

每次访问时改变 `v=` 后面的值即可。

## 📝 验证是否成功

清除缓存后，检查以下内容：

### ✅ 必须看到的元素

1. **页面标题**: 
   - ✅ 应该显示: `支撑压力线系统 [v2.0-12小时分页]`
   - ❌ 如果没有 `[v2.0-12小时分页]`，说明缓存未清除

2. **翻页按钮**:
   ```
   ← 前12小时  |  2025-12-12 00:00 - 12:00  |  后12小时 →
   ```
   - ✅ 应该看到两个按钮
   - ❌ 如果看不到按钮，说明缓存未清除

3. **浏览器标题栏**:
   - ✅ 应该显示: `支撑压力线系统 - 27币种实时监控 v2.0`
   - ❌ 如果没有 `v2.0`，说明缓存未清除

### 🧪 功能测试

1. **选择历史日期** (如 2025-12-12)
2. **查看按钮状态**:
   - "前12小时"按钮: 应该是灰色/禁用（因为在第0页）
   - "后12小时"按钮: 应该是亮色/可点击
3. **点击"后12小时 →"**:
   - 页面应该切换到 "2025-12-12 12:00 - 24:00"
   - 图表应该更新显示新的时间范围
4. **打开浏览器控制台** (F12 → Console):
   ```javascript
   📊 按钮状态更新: {
       日期: "2025-12-12",
       是否今天: false,  // ✅ 应该是false
       当前小时: 2,
       当前页: 0,
       后12小时按钮禁用: false  // ✅ 应该是false（启用）
   }
   ```

## 🛠️ 如果还是不行

### 最后手段: 完全清除浏览器缓存

#### Chrome / Edge:
1. 按 `Ctrl + Shift + Delete`（Mac: `Cmd + Shift + Delete`）
2. 选择"时间范围": 全部时间
3. 勾选:
   - ✅ 浏览历史记录
   - ✅ 缓存的图片和文件
   - ✅ Cookie和其他网站数据
4. 点击"清除数据"
5. 重启浏览器

#### Firefox:
1. 按 `Ctrl + Shift + Delete`
2. 选择"时间范围": 全部
3. 勾选:
   - ✅ 浏览和下载历史
   - ✅ Cookie
   - ✅ 缓存
4. 点击"立即清除"
5. 重启浏览器

## 🎯 推荐流程

```
1. 先尝试: Ctrl + Shift + R (强制刷新)
   ↓
2. 如果不行: F12 → 禁用缓存 → F5
   ↓
3. 如果还不行: 清除特定网站数据
   ↓
4. 最后手段: 隐私模式访问
```

## 📱 移动端

### iOS Safari:
1. 设置 → Safari → 清除历史记录与网站数据
2. 或长按刷新按钮选择"重新加载网页"

### Android Chrome:
1. 设置 → 隐私和安全 → 清除浏览数据
2. 选择"缓存的图片和文件"
3. 清除数据

## 🔗 验证链接

**测试页面**:
https://5000-iypypqmz2wvn9dmtq7ewn-583b4d74.sandbox.novita.ai/support-resistance

**预期看到**:
- ✅ 标题包含 `[v2.0-12小时分页]`
- ✅ 有"前12小时"和"后12小时"按钮
- ✅ 按钮可以正常点击和切换
- ✅ X轴显示北京时间（如 08:00, 09:00）

---

**如果按照本指南操作后仍有问题，请截图发送以下内容**:
1. 页面完整截图（包括标题和按钮区域）
2. F12控制台的日志输出
3. 浏览器版本和操作系统信息
