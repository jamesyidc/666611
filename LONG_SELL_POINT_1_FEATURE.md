# 做多卖点1标记功能文档

## 功能概述

在K线图上自动检测并标记"做多卖点1"，帮助用户识别潜在的做多平仓时机。

## 📋 做多卖点1定义

### 完整条件
**做多卖点1：** 在5分钟周期K线图中，30根K线的最高点之后的6根K线内，没有出现连续3个满足震荡条件的K线。

### 条件拆解

1. **窗口范围**: 30根K线
2. **最高点识别**: 找到这30根K线中的最高价格点
3. **观察期**: 最高点之后的6根K线
4. **震荡条件**: 
   - 震荡幅度 ≤ 0.5%
   - 涨跌幅 ≤ +0.25%
5. **触发条件**: 观察期内**没有**连续3个满足震荡条件的K线

## 🎯 检测逻辑

### 算法流程

```javascript
// 1. 遍历每根K线（从第30根开始，确保有完整的30根窗口）
for (let i = 29; i < ohlc.length - 6; i++) {
    
    // 2. 找到前30根K线的最高点
    let maxHighIdx = i - 29;
    let maxHigh = ohlc[i - 29][3];  // [open, close, low, high]
    
    for (let j = i - 28; j <= i; j++) {
        if (ohlc[j][3] > maxHigh) {
            maxHigh = ohlc[j][3];
            maxHighIdx = j;
        }
    }
    
    // 3. 如果当前位置是30根K线的最高点
    if (maxHighIdx === i) {
        
        // 4. 检查后6根K线是否有连续3个震荡
        let hasConsecutive3Oscillation = false;
        let oscCount = 0;
        
        for (let k = i + 1; k <= i + 6 && k < ohlc.length; k++) {
            const [open, close, low, high] = ohlc[k];
            const changePct = (close - open) / open * 100;
            const volatilityPct = (high - low) / open * 100;
            
            // 检查是否满足震荡条件
            if (changePct <= 0.25 && volatilityPct <= 0.50) {
                oscCount++;
                if (oscCount >= 3) {
                    hasConsecutive3Oscillation = true;
                    break;
                }
            } else {
                oscCount = 0;  // 不连续则重置计数
            }
        }
        
        // 5. 如果后6根K线没有连续3个震荡，标记为卖点1
        if (!hasConsecutive3Oscillation) {
            longSellPoint1Markers.push({
                coord: [i, maxHigh],
                value: maxHigh.toFixed(4),
                xIndex: i
            });
        }
    }
}
```

## 📊 图表标记样式

### 标记外观
- **符号**: 🔻 + "卖1"
- **颜色**: 红色 (#ff4444)
- **位置**: 标记在最高点K线的顶部
- **标签格式**: 
  ```
  🔻卖1
  价格
  ```

### 标记示例
```
     🔻卖1
      5.62
        │
        ▼
     ╔═══╗
     ║ K ║  ← 30根K线的最高点
     ╚═══╝
```

## 💡 使用场景

### 场景1：顶部卖出信号
```
情况：价格创新高后持续下跌或震荡
标记：🔻卖1
含义：可能是做多的退出时机
```

### 场景2：假突破识别
```
情况：价格突破后快速回落
标记：🔻卖1
含义：假突破，建议平仓
```

### 场景3：趋势转折
```
情况：上涨趋势结束，开始横盘或下跌
标记：🔻卖1
含义：趋势可能反转，考虑减仓
```

## 🔍 判断标准

### 震荡条件详解

**单根K线的震荡条件：**
```javascript
震荡幅度 = (最高价 - 最低价) / 开盘价 × 100%  ≤ 0.5%
涨跌幅 = (收盘价 - 开盘价) / 开盘价 × 100%  ≤ +0.25%
```

**连续3个震荡：**
- 连续3根K线都满足上述条件
- 中间不能中断（否则重新计数）

### 示例分析

#### 示例1：触发卖点1 ✅
```
K线1-30: 最高点在第30根，价格 = $5.62
K线31-36: 
  第31根: 跌-0.5% (不满足震荡)
  第32根: 跌-0.3% (不满足震荡)
  第33根: 跌-0.2% (满足震荡) ← 计数1
  第34根: 涨+0.15% (满足震荡) ← 计数2
  第35根: 涨+0.8% (不满足震荡) ← 计数重置
  第36根: 无连续3个震荡

结果: 触发做多卖点1 🔻
```

#### 示例2：不触发卖点1 ❌
```
K线1-30: 最高点在第30根，价格 = $5.62
K线31-36:
  第31根: 跌-0.1% (满足震荡) ← 计数1
  第32根: 涨+0.1% (满足震荡) ← 计数2
  第33根: 平0% (满足震荡) ← 计数3 ✅ 连续3个！
  
结果: 不触发卖点1（因为有连续3个震荡）
```

## 📈 K线图显示

### 查看方式
1. 访问任意币种的K线图
2. 选择"5分钟"时间周期
3. 红色🔻卖1标记会自动显示在符合条件的位置

### 标记特征
- **标记位置**: 30根K线的最高点
- **标记颜色**: 红色（警示色）
- **标记符号**: 🔻（向下箭头）
- **标记文本**: "卖1"
- **价格显示**: 显示该点的最高价

## 🔧 技术实现

### 实现文件
- `templates/symbol_detail_v6.html` (第777-832行)

### 关键代码
```javascript
// 检测做多卖点1
const longSellPoint1Markers = [];

for (let i = 29; i < ohlc.length - 6; i++) {
    // 找30根K线最高点
    // 检查后6根是否有连续3个震荡
    // 如果没有，标记为卖点1
}

// 添加到图表标记
markPoint: {
    data: [
        ...markPoints,
        ...longSellPoint1Markers.map(marker => ({
            coord: [marker.xIndex, marker.coord[1]],
            value: '卖1',
            itemStyle: { color: '#ff4444' },
            label: {
                formatter: '🔻卖1\n{c}',
                backgroundColor: '#ff4444'
            }
        }))
    ]
}
```

## 📊 与其他标记的关系

### 当前K线图标记系统

| 标记类型 | 符号 | 颜色 | 含义 |
|---------|------|------|------|
| 48H高 | 🔴 | 红色 | 48小时最高点 |
| 48H低 | 🔵 | 蓝色 | 48小时最低点 |
| 7D高 | 🟡 | 黄色 | 7天最高点 |
| 7D低 | 🟢 | 绿色 | 7天最低点 |
| 卖1 | 🔻 | 红色 | **做多卖点1** |

### 标记优先级
1. 做多卖点1（动态信号）
2. 48H/7D高低点（统计信号）
3. 低波动震荡区域（背景标记）

## 🎮 调试信息

### Console日志
```javascript
console.log('[做多卖点1检测] 符合条件的卖点:', longSellPoint1Markers);
```

### 输出格式
```javascript
[{
    coord: [156, 5.6234],  // [K线索引, 最高价]
    value: "5.6234",       // 最高价（字符串）
    xIndex: 156            // K线索引
}, ...]
```

## 📍 访问地址

### BTC K线图（5分钟周期）
https://5000-iypypqmz2wvn9dmtq7ewn-583b4d74.sandbox.novita.ai/symbol/BTC/v6

### ETH K线图（5分钟周期）
https://5000-iypypqmz2wvn9dmtq7ewn-583b4d74.sandbox.novita.ai/symbol/ETH/v6

### UNI K线图（5分钟周期）
https://5000-iypypqmz2wvn9dmtq7ewn-583b4d74.sandbox.novita.ai/symbol/UNI/v6

### 所有币种列表
https://5000-iypypqmz2wvn9dmtq7ewn-583b4d74.sandbox.novita.ai/

## ⚠️ 注意事项

### 1. 时间周期限制
- 仅在**5分钟周期**下有效
- 其他周期（1小时、4小时）不适用此逻辑

### 2. 数据完整性要求
- 至少需要36根K线数据（30根窗口 + 6根观察期）
- 数据不足时不会显示标记

### 3. 实时更新
- 随K线数据实时更新
- 翻页后标记保持一致（基于全局数据计算）

### 4. 标记含义
- 这是**建议性信号**，不是强制指令
- 需结合其他指标综合判断
- 适用于5分钟短线交易策略

## 📚 相关文档

1. `OSCILLATION_CONDITION_NAMING.md` - 震荡条件命名规范
2. `KLINE_OSCILLATION_LOGIC_FIX.md` - 震荡检测逻辑修正
3. `KLINE_HIGHLOW_MARKERS_FEATURE.md` - K线高低点标记功能

## 🔄 后续优化建议

### 功能扩展
1. 添加更多卖点类型（卖点2、卖点3）
2. 提供卖点强度评分
3. 支持自定义参数（窗口大小、观察期长度）
4. 添加卖点历史统计和回测

### 显示优化
1. 鼠标悬停显示详细信息
2. 卖点标记点击跳转到详情
3. 不同强度的卖点使用不同颜色
4. 添加卖点触发原因说明

## 📊 性能指标

- **检测范围**: 当前页面所有K线数据
- **计算复杂度**: O(n × 30)，n为K线数量
- **标记数量**: 通常每页0-5个
- **渲染性能**: 对图表性能影响 < 5%

---

**版本信息**
- 功能版本: v1.0
- 实现日期: 2025-12-12
- 适用周期: 5分钟K线
- 影响范围: 所有27个币种

**开发完成** ✅  
做多卖点1标记功能已实现并在所有币种K线图上生效。
