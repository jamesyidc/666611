# 🔄 强制刷新指南 - 清除浏览器缓存

## ⚠️ 如果您看到旧版本页面

### 症状检查

如果您看到以下任何一项，说明**浏览器缓存了旧版本**：

❌ **旧版本标志**:
- 标题是 `v2.0` 或更早版本
- 按钮标签是 "← 前12小时" 和 "后12小时 →"
- 没有绿色的 "🆕 v2.1 新功能" 提示条
- 页面顶部有**红色警告条**

✅ **新版本标志**:
- 标题是 `v2.1-最新数据` 或 `[v2.1-自动显示最新数据 🆕]`
- 按钮标签是 "← 更早" 和 "更晚 →"
- 有绿色提示条显示新功能
- 页面自动显示当前时间（如 2025-12-13）

---

## 🚀 快速清除缓存方法

### 方法1: 硬性重新加载（最快）⭐

**Windows / Linux**:
```
按住 Ctrl + Shift + R
或
按住 Ctrl + F5
```

**Mac**:
```
按住 Cmd + Shift + R
或
按住 Cmd + Option + R
```

### 方法2: 开发者工具清除（最彻底）⭐⭐⭐

#### Chrome / Edge:
1. 按 `F12` 打开开发者工具
2. **右键点击**地址栏旁边的刷新按钮 🔄
3. 选择 **"清空缓存并硬性重新加载"**
4. 或者：
   - 开发者工具打开时，按 `Ctrl + Shift + R`

#### Firefox:
1. 按 `F12` 打开开发者工具
2. 在开发者工具中，点击 "网络" 标签
3. 勾选 "禁用缓存"
4. 刷新页面 (`F5` 或 `Ctrl+R`)

#### Safari:
1. 开启开发菜单: 偏好设置 → 高级 → 勾选"在菜单栏中显示开发菜单"
2. 开发 → 清空缓存
3. 按 `Cmd + R` 刷新页面

### 方法3: 隐私模式访问（测试用）⭐⭐

**Chrome / Edge**:
```
Ctrl + Shift + N  (Windows/Linux)
Cmd + Shift + N   (Mac)
```

**Firefox**:
```
Ctrl + Shift + P  (Windows/Linux)
Cmd + Shift + P   (Mac)
```

**Safari**:
```
Cmd + Shift + N
```

然后在隐私窗口中访问:
```
https://5000-iypypqmz2wvn9dmtq7ewn-583b4d74.sandbox.novita.ai/support-resistance
```

---

## 🔍 验证清除是否成功

清除缓存后，检查以下内容：

### ✅ 必须看到的元素

1. **页面标题**（浏览器标签）:
   ```
   支撑压力线系统 - 27币种实时监控 v2.1-最新数据
   ```

2. **页面标题**（页面内）:
   ```
   📊 支撑压力线系统 [v2.1-自动显示最新数据 🆕]
   ```

3. **按钮标签**:
   ```
   ← 更早  |  2025-12-13 00:00 - 12:00  |  更晚 →
   ```

4. **绿色提示条**（在图表下方）:
   ```
   🆕 v2.1 新功能: 自动显示当前时间数据 | 按钮改为 "← 更早" 和 "更晚 →"
   ```

5. **当前日期**:
   - 应该显示 **2025-12-13**（今天）
   - 而不是 2025-12-12 或更早的日期

### ❌ 如果仍看到旧版本

页面顶部会显示**红色警告条**:
```
⚠️ 页面版本过旧！请按 Ctrl+Shift+R 强制刷新清除缓存 ⚠️
```

如果看到这个，请：
1. 再次按 `Ctrl + Shift + R`
2. 或尝试隐私模式访问
3. 或清除浏览器所有缓存（见下文）

---

## 🧪 控制台验证

1. 按 `F12` 打开开发者工具
2. 点击 "Console" 或 "控制台" 标签
3. 查看日志输出

### ✅ 新版本日志:
```javascript
✅ 页面版本: v2.1 (最新)
✅ 按钮标签已更新: ← 更早 / 更晚 →
🚀 页面初始化: {
    当前日期: "2025-12-13",
    当前页面: 0,
    页面说明: "00:00-12:00 (前12小时)"
}
```

### ❌ 旧版本日志:
```javascript
⚠️ 检测到旧版本页面！请清除浏览器缓存！
清除方法: Ctrl+Shift+R (Windows) 或 Cmd+Shift+R (Mac)
```

---

## 🛠️ 完全清除浏览器缓存

如果上述方法都不行，使用此方法：

### Chrome / Edge:
1. 按 `Ctrl + Shift + Delete`（Mac: `Cmd + Shift + Delete`）
2. 时间范围：选择 **"全部"** 或 **"最近1小时"**
3. 勾选:
   - ✅ 浏览历史记录
   - ✅ Cookie 和其他网站数据
   - ✅ 缓存的图片和文件
4. 点击 **"清除数据"**
5. 关闭并重新打开浏览器
6. 重新访问页面

### Firefox:
1. 按 `Ctrl + Shift + Delete`
2. 时间范围：选择 **"全部"**
3. 勾选:
   - ✅ 浏览和下载历史
   - ✅ Cookie
   - ✅ 缓存
4. 点击 **"立即清除"**
5. 重启浏览器

### Safari:
1. Safari 菜单 → 偏好设置 → 隐私
2. 点击 **"管理网站数据"**
3. 点击 **"移除全部"**
4. 或：Safari → 清除历史记录 → 选择 "所有历史记录"

---

## 📱 移动端清除缓存

### iOS Safari:
1. 设置 → Safari → 清除历史记录与网站数据
2. 或：长按刷新按钮 → 选择 "重新加载"

### Android Chrome:
1. 设置 → 隐私和安全 → 清除浏览数据
2. 选择 "缓存的图片和文件"
3. 清除数据

---

## 🎯 特定网站清除（推荐）

只清除特定网站的缓存，不影响其他网站：

### Chrome / Edge:
1. 按 `F12` 打开开发者工具
2. 点击 "Application" 或 "应用" 标签
3. 左侧找到 "Storage" 或 "存储"
4. 点击 **"Clear site data"** 或 **"清除网站数据"**
5. 刷新页面

### Firefox:
1. 点击地址栏前的锁图标 🔒
2. 点击 "清除 Cookie 和网站数据"
3. 点击 "清除"
4. 刷新页面

---

## 🔗 直接访问链接

**主页面**:
https://5000-iypypqmz2wvn9dmtq7ewn-583b4d74.sandbox.novita.ai/support-resistance

**带时间戳的链接**（破坏缓存）:
```
https://5000-iypypqmz2wvn9dmtq7ewn-583b4d74.sandbox.novita.ai/support-resistance?v=20251213-v21
```

---

## 📊 版本对比

| 特性 | 旧版本 (v2.0及以前) | 新版本 (v2.1) |
|-----|-------------------|--------------|
| 默认日期 | 2025-12-12（过去） | 2025-12-13（今天）✅ |
| 默认时间 | 00:00-12:00 | 自动选择当前时间段 ✅ |
| 按钮标签 | 前12小时/后12小时 | 更早/更晚 ✅ |
| 缓存检测 | 无 | 自动检测+红色警告 ✅ |
| 版本提示 | v2.0-12小时分页 | v2.1-自动显示最新数据 ✅ |

---

## 🆘 仍然有问题？

如果尝试所有方法后仍看不到新版本：

1. **检查网络连接**: 确保能正常访问网站
2. **尝试其他浏览器**: Chrome / Firefox / Edge / Safari
3. **检查控制台错误**: F12 → Console 查看是否有错误
4. **截图并反馈**: 
   - 完整页面截图
   - F12 控制台截图
   - 浏览器版本信息

---

**记住**: `Ctrl + Shift + R` 是最快的方法！

**如果成功**: 您应该看到绿色的 "🆕 v2.1 新功能" 提示条，按钮标签变为 "← 更早" 和 "更晚 →"
