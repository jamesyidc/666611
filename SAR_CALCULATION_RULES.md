# SAR斜率系统 - 完整计算规则文档

## 📋 目录
1. [基础规则](#基础规则)
2. [数据显示规则](#数据显示规则)
3. [SAR差值计算规则](#sar差值计算规则)
4. [变化率计算规则](#变化率计算规则)
5. [历史平均值计算规则](#历史平均值计算规则)
6. [偏向判断规则](#偏向判断规则)
7. [数据库结构](#数据库结构)
8. [API接口规则](#api接口规则)

---

## 基础规则

### 1.1 数据采集周期
- **K线周期**: 5分钟
- **数据更新频率**: 每5分钟采集一次
- **支持币种**: 27个加密货币（BTC, ETH, AAVE等）
- **数据保留**: 目标16天（实际约5天，受API限制）

### 1.2 SAR序列定义
- **多头序列**: 当SAR指标显示多头时，从多头01开始计数
- **空头序列**: 当SAR指标显示空头时，从空头01开始计数
- **序列转换**: 当多头转为空头，或空头转为多头时，序列重新从01开始

---

## 数据显示规则

### 2.1 时间排序规则
**重要**: 数据必须按时间**倒序**显示

```
✅ 正确显示顺序（时间倒序）:
空08 | 11:40:00  ← 最新数据在最上面
空07 | 11:35:00
空06 | 11:30:00
空05 | 11:25:00
...
空01 | 11:05:00  ← 最早数据在最下面

❌ 错误显示顺序（时间正序）:
空01 | 11:05:00  ← 最早
...
空08 | 11:40:00  ← 最新
```

**用户要求**: 
- "时间越早的往下"
- "离现在越近的在前"

### 2.2 完整周期显示规则
- **空头周期**: 显示 空头01 → 空头02 → ... → 空头N (直到转为多头)
- **多头周期**: 显示 多头01 → 多头02 → ... → 多头N (直到转为空头)
- **不显示**: 持续时间字段（已移除）

---

## SAR差值计算规则

### 3.1 SAR差值定义
SAR差值 = 相邻两个序列的SAR值的绝对差值

### 3.2 多头SAR差值计算公式
```
SAR差值 = 当前序列SAR - 前一个序列SAR

例如（按显示顺序，时间倒序）:
多头03 SAR = 151.1207
多头02 SAR = 151.0451
SAR差值 = 151.1207 - 151.0451 = 0.0756
```

### 3.3 空头SAR差值计算公式
```
SAR差值 = 前一个序列SAR - 当前序列SAR

例如（按显示顺序，时间倒序）:
空头08 SAR = 150.9740  ← 新序列（时间更晚）
空头07 SAR = 151.0451  ← 旧序列（时间更早）
SAR差值 = 151.0451 - 150.9740 = 0.0711

注意：虽然空08在上面显示，但计算时用的是"前一个"（序列号小的）减去"当前"（序列号大的）
```

### 3.4 代码实现逻辑（时间倒序）
```python
# 数据已经按时间倒序排列：新 → 旧
# raw_sequences[0] = 空08 (最新)
# raw_sequences[1] = 空07 (较旧)

for i, seq_data in enumerate(raw_sequences):
    if i < len(raw_sequences) - 1:
        curr_sar = seq_data['sar']              # 当前（新，序列号大）
        next_sar = raw_sequences[i+1]['sar']   # 下一个（旧，序列号小）
        
        if position == 'long':
            sar_diff = curr_sar - next_sar      # 多头：新 - 旧
        else:  # short
            sar_diff = next_sar - curr_sar      # 空头：旧 - 新
```

---

## 变化率计算规则

### 4.1 变化率定义
变化率% = (SAR差值 / 除数SAR) × 100

### 4.2 多头变化率计算公式
```
变化率% = (多头03 - 多头02) / 多头03 × 100

例如:
多头03 SAR = 151.1207
多头02 SAR = 151.0451
变化率% = (151.1207 - 151.0451) / 151.1207 × 100
       = 0.0756 / 151.1207 × 100
       = 0.0500%
```

**用户原话**: "多头是（多头03-多头02）/多头03"

### 4.3 空头变化率计算公式
```
变化率% = (空头02 - 空头03) / 空头02 × 100

例如（按显示顺序，时间倒序）:
空头08 SAR = 150.9740  ← 新序列（时间11:40）
空头07 SAR = 151.0451  ← 旧序列（时间11:35）

变化率% = (151.0451 - 150.9740) / 151.0451 × 100
       = 0.0711 / 151.0451 × 100
       = 0.0471%

实际计算：用"旧序列（序列号小）"除
```

**用户原话**: "空头是（空头02-空头03）/空头02"

### 4.4 代码实现逻辑（时间倒序）
```python
# 空头情况，数据倒序：空08(新) → 空07(旧)
if position == 'short':
    # next_sar 是旧的（序列号小，如空07）
    # curr_sar 是新的（序列号大，如空08）
    # 公式：(旧 - 新) / 旧
    change_percent = ((next_sar - curr_sar) / next_sar) * 100
    
# 多头情况
if position == 'long':
    # 公式：(新 - 旧) / 新
    change_percent = ((curr_sar - next_sar) / curr_sar) * 100
```

### 4.5 完整验证示例
```
空头序列（时间倒序显示）:
┌────────┬────────┬────────────┬──────────┬──────────┬─────────────────────────┐
│ 序列   │ 时间   │ SAR值      │ SAR差值  │ 变化率%  │ 计算公式                │
├────────┼────────┼────────────┼──────────┼──────────┼─────────────────────────┤
│ 空08   │ 11:40  │ 150.9740   │ 0.0711   │ 0.0471%  │ (151.0451-150.9740)/    │
│        │        │            │          │          │  151.0451×100           │
├────────┼────────┼────────────┼──────────┼──────────┼─────────────────────────┤
│ 空07   │ 11:35  │ 151.0451   │ 0.0756   │ 0.0500%  │ (151.1207-151.0451)/    │
│        │        │            │          │          │  151.1207×100           │
├────────┼────────┼────────────┼──────────┼──────────┼─────────────────────────┤
│ 空06   │ 11:30  │ 151.1207   │ 0.0454   │ 0.0301%  │ (151.1662-151.1207)/    │
│        │        │            │          │          │  151.1662×100           │
└────────┴────────┴────────────┴──────────┴──────────┴─────────────────────────┘

✅ 所有计算遵循规则: (前序列SAR - 后序列SAR) / 前序列SAR × 100
```

---

## 历史平均值计算规则

### 5.1 平均值周期定义
```
1天平均  = 最近288条记录的平均值  (24小时 × 12条/小时 = 288)
3天平均  = 最近864条记录的平均值  (3天 × 288 = 864)
7天平均  = 最近2016条记录的平均值 (7天 × 288 = 2016)
15天平均 = 最近4320条记录的平均值 (15天 × 288 = 4320)
```

### 5.2 平均值计算数据源
- 从 `sar_consecutive_changes` 表获取历史数据
- 根据 `symbol`, `position`, `sequence_num` 筛选
- 按 `id DESC` 排序（最新的在前）

### 5.3 平均值计算示例
```sql
-- 获取空头序列8的历史数据
SELECT change_percent
FROM sar_consecutive_changes
WHERE symbol = 'AAVE' 
  AND position = 'short' 
  AND sequence_num = 8
ORDER BY id DESC
LIMIT 288;  -- 1天数据

-- 计算平均值
1天平均 = SUM(change_percent) / COUNT(*)
```

### 5.4 变化百分比计算规则 ⭐【重要更新】
```
【规则】相对变化百分比计算公式：

1天变化% = (当前序列变化率 - 1天平均变化率) / 1天平均变化率 × 100
3天变化% = (当前序列变化率 - 3天平均变化率) / 3天平均变化率 × 100
7天变化% = (当前序列变化率 - 7天平均变化率) / 7天平均变化率 × 100

这是相对于平均值的百分比变化，不是绝对差值！
```

#### 计算示例1（空10）
```
当前变化率 = 0.0613%
1天平均   = 0.083284%

绝对差值 = 0.0613% - 0.083284% = -0.021984%

1天变化% = (0.0613 - 0.083284) / 0.083284 × 100
         = -0.021984 / 0.083284 × 100
         = -26.40%

解释: 当前变化率比1天平均低26.40%
```

#### 计算示例2（空09）
```
当前变化率 = 0.0443%
1天平均   = 0.082003%

1天变化% = (0.0443 - 0.082003) / 0.082003 × 100
         = -0.037703 / 0.082003 × 100
         = -45.98%

解释: 当前变化率比1天平均低45.98%
```

#### 正负值含义
```
1天变化% 为负数: 当前变化率 < 历史平均，SAR变化放缓
1天变化% 为正数: 当前变化率 > 历史平均，SAR变化加速
1天变化% 接近0:  当前变化率 ≈ 历史平均，趋势稳定
```

---

## 偏向判断规则

### 6.1 多头区间偏向判断
```
if 当前方向 == '多头':
    if 1天差值% < 0:
        偏向 = '偏多'
        说明 = '多头变化率减小，趋势稳健'
    else:
        偏向 = '偏空'
        说明 = '多头变化率增大，可能加速赶顶'
```

### 6.2 空头区间偏向判断
```
if 当前方向 == '空头':
    if 1天差值% < 0:
        偏向 = '偏空'
        说明 = '空头变化率减小，趋势稳健'
    else:
        偏向 = '偏多'
        说明 = '空头变化率增大，可能加速赶底'
```

### 6.3 偏向判断逻辑图
```
多头区间:
  变化率减小（差值<0） → 偏多 🟢 (趋势稳健)
  变化率增大（差值>0） → 偏空 🔴 (可能见顶)

空头区间:
  变化率减小（差值<0） → 偏空 🔴 (趋势稳健)
  变化率增大（差值>0） → 偏多 🟢 (可能见底)
```

---

## 数据库结构

### 7.1 主要数据表

#### sar_raw_data (原始SAR数据)
```sql
CREATE TABLE sar_raw_data (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    symbol TEXT NOT NULL,
    timestamp INTEGER NOT NULL,
    kline_time TEXT NOT NULL,
    open_price REAL,
    high_price REAL,
    low_price REAL,
    close_price REAL,              -- 币价
    sar_value REAL,                -- SAR值
    position TEXT,                 -- 'long' 或 'short'
    position_sequence INTEGER,     -- 序列号
    duration_minutes INTEGER,      -- 持续时间（分钟）
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### sar_consecutive_changes (连续变化率)
```sql
CREATE TABLE sar_consecutive_changes (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    symbol TEXT NOT NULL,
    position TEXT NOT NULL,        -- 'long' 或 'short'
    sequence_num INTEGER NOT NULL, -- 序列号
    change_percent REAL,           -- 变化率%
    kline_time TEXT,
    duration_minutes INTEGER,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### sar_period_averages (周期平均值)
```sql
CREATE TABLE sar_period_averages (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    symbol TEXT NOT NULL,
    position TEXT NOT NULL,
    period_type TEXT NOT NULL,     -- '1day', '3day', '7day', '15day'
    avg_change_percent REAL,
    sample_count INTEGER,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### system_status (系统状态)
```sql
CREATE TABLE system_status (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    symbol TEXT NOT NULL UNIQUE,
    last_update_time TIMESTAMP,
    last_kline_time TEXT,
    total_klines INTEGER,
    current_position TEXT,         -- 当前方向
    current_sequence INTEGER,      -- 当前序列号
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

---

## API接口规则

### 8.1 主要API端点

#### GET /api/sar-slope/current-cycle/<symbol>
获取当前完整周期的所有序列数据

**返回数据结构**:
```json
{
  "success": true,
  "symbol": "AAVE",
  "current_status": {
    "position": "short",
    "position_cn": "空头",
    "current_sequence": 8,
    "last_update": "2025-12-25 11:40:00",
    "cycle_info": "short01 → short08"
  },
  "sequences": [
    {
      "sequence": 8,
      "time": "2025-12-25 11:40:00",
      "price": 150.03,
      "sar": 150.9740,
      "sar_diff": 0.0711,                    // SAR绝对差值
      "sequence_change_percent": 0.0471,     // 变化率%
      "avg_1day": 0.0770,                    // 1天平均
      "avg_3day": 0.0770,                    // 3天平均
      "avg_7day": 0.0770,                    // 7天平均
      "diff_1day": -0.0299,                  // 1天差值
      "diff_3day": -0.0299,                  // 3天差值
      "diff_7day": -0.0299,                  // 7天差值
      "bias": "偏空"                         // 偏向
    },
    ...
  ],
  "total_sequences": 8
}
```

**数据顺序**: 时间倒序（最新在前）

### 8.2 网页展示规则

#### 表格列定义（共11列）
```
1. 序列      - 显示格式: "空01", "空02", "多01", "多02"
2. 时间      - 显示格式: "11:40" (HH:MM)
3. 币价      - 显示格式: "$150.03" (保留2位小数)
4. SAR值     - 显示格式: "150.9740" (保留4位小数)
5. SAR差值   - 显示格式: "0.0711" (保留4位小数)
6. 变化率%   - 显示格式: "0.0471%" (保留4位小数)
7. 1天变化%  - 显示格式: "-0.0299%" (保留4位小数，带+/-号)
8. 1天平均   - 显示格式: "0.077020%" (保留6位小数)
9. 3天变化%  - 显示格式: "-0.0299%" (保留4位小数，带+/-号)
10. 3天平均  - 显示格式: "0.077020%" (保留6位小数)
11. 偏向     - 显示格式: "偏多" 或 "偏空"
```

#### 颜色标识规则
```css
/* 多头/空头颜色 */
.position-long  { color: #00ff00; }  /* 绿色 */
.position-short { color: #ff4444; }  /* 红色 */

/* 变化趋势颜色 */
.trend-increase { color: #ff4444; }  /* 增加 - 红色 */
.trend-decrease { color: #00ff00; }  /* 减少 - 绿色 */

/* 偏向颜色 */
.bias-bullish   { color: #00ff00; }  /* 偏多 - 绿色 */
.bias-bearish   { color: #ff4444; }  /* 偏空 - 红色 */
```

---

## 完整计算流程图

```
┌─────────────────────────────────────────────────────────────────┐
│ 1. 数据采集 (每5分钟)                                           │
│    - 获取K线数据                                                │
│    - 计算SAR指标                                                │
│    - 确定多空方向和序列号                                       │
└────────────────────────┬────────────────────────────────────────┘
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│ 2. SAR差值计算                                                  │
│    多头: SAR差值 = 当前SAR - 前一个SAR                         │
│    空头: SAR差值 = 前一个SAR - 当前SAR                         │
└────────────────────────┬────────────────────────────────────────┘
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│ 3. 变化率计算                                                   │
│    多头: 变化率% = (当前SAR - 前SAR) / 当前SAR × 100          │
│    空头: 变化率% = (前SAR - 当前SAR) / 前SAR × 100            │
└────────────────────────┬────────────────────────────────────────┘
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│ 4. 历史平均值计算                                               │
│    - 获取同序列号的历史数据                                     │
│    - 计算1天/3天/7天/15天平均                                  │
└────────────────────────┬────────────────────────────────────────┘
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│ 5. 差值计算                                                     │
│    1天差值% = 当前变化率 - 1天平均                             │
│    3天差值% = 当前变化率 - 3天平均                             │
│    7天差值% = 当前变化率 - 7天平均                             │
└────────────────────────┬────────────────────────────────────────┘
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│ 6. 偏向判断                                                     │
│    多头: 差值<0→偏多, 差值>0→偏空                             │
│    空头: 差值<0→偏空, 差值>0→偏多                             │
└────────────────────────┬────────────────────────────────────────┘
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│ 7. 数据展示（时间倒序）                                         │
│    最新序列在上，最早序列在下                                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 关键注意事项

### ⚠️ 重要规则
1. **时间排序**: 必须倒序显示（最新在上）
2. **变化率分母**: 
   - 多头用"当前序列"（序列号大）作分母
   - 空头用"前序列"（序列号小）作分母
3. **SAR差值**: 始终是"大SAR - 小SAR"的绝对值
4. **序列号**: 从01开始，多空转换时重置

### 🔍 调试检查点
- [ ] 时间是否倒序排列
- [ ] SAR差值是否为正数
- [ ] 变化率公式是否正确（注意分母）
- [ ] 平均值计算是否使用正确的数据量
- [ ] 偏向判断逻辑是否正确

---

## 文档版本
- **版本**: 1.0
- **更新日期**: 2025-12-25
- **作者**: SAR斜率系统开发团队
- **最后修改**: 根据用户反馈完善所有计算规则

---

## 附录：常见问题

### Q1: 为什么空头的变化率用"前序列"做分母？
A: 这是用户明确要求的计算方式："空头是（空头02-空头03）/空头02"，其中空头02是前序列（序列号小）。

### Q2: 时间倒序显示会影响计算吗？
A: 不影响。计算逻辑已经适配倒序显示，通过判断序列号大小来确定"前"和"后"。

### Q3: SAR差值为什么有时候很小？
A: SAR值通常在100以上，相邻序列的SAR值差异通常在0.01~0.1之间，这是正常现象。

### Q4: 如何验证计算是否正确？
A: 手动计算几个序列的值，与API返回值对比，确保一致。参考本文档第4.5节的验证示例。

---

**本文档包含SAR斜率系统的所有核心计算规则，请妥善保存！**
