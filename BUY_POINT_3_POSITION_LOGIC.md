# 买点3仓位计算逻辑说明

## 📊 核心规则

买点3（空转多买入）的仓位计算依赖于**开仓逻辑页面**的建议：

### 情况1：开仓逻辑【不允许开仓】
- **条件**：大趋势、中趋势、小趋势都不允许开仓
- **买点3仓位**：额外开 **20%**
- **首次开仓**：总仓位 × 30% = **6%**

### 情况2：开仓逻辑【允许开仓】
- **条件**：开仓逻辑建议做多且有仓位建议
- **买点3仓位**：**开仓逻辑建议仓位 + 20%**，最高不超过 **70%**
- **公式**：`min(开仓逻辑仓位 + 20, 70)`

## 🎯 实际案例

### 案例1：开仓逻辑不允许（震荡行情）
```
开仓逻辑：❌ 不允许做多（震荡观望）
买点3触发：✅ 满足5个严格条件
-----------------
买点3建议仓位：20%
首次开仓建议：6% (20% × 30%)
计算说明：开仓逻辑不允许，买点3可额外开20%
```

### 案例2：开仓逻辑允许 - 10%仓位
```
开仓逻辑：✅ 一般做多 - 建议10%
买点3触发：✅ 满足5个严格条件
-----------------
买点3建议仓位：30% (10% + 20%)
首次开仓建议：9% (30% × 30%)
计算说明：开仓逻辑10% + 买点3加成20% = 30%
```

### 案例3：开仓逻辑允许 - 20%仓位
```
开仓逻辑：✅ 一般做多 - 建议20%
买点3触发：✅ 满足5个严格条件
-----------------
买点3建议仓位：40% (20% + 20%)
首次开仓建议：12% (40% × 30%)
计算说明：开仓逻辑20% + 买点3加成20% = 40%
```

### 案例4：开仓逻辑允许 - 40%仓位
```
开仓逻辑：✅ 较强做多 - 建议40%
买点3触发：✅ 满足5个严格条件
-----------------
买点3建议仓位：60% (40% + 20%)
首次开仓建议：18% (60% × 30%)
计算说明：开仓逻辑40% + 买点3加成20% = 60%
```

### 案例5：开仓逻辑允许 - 60%仓位（触发上限）
```
开仓逻辑：✅ 很强做多 - 建议60%
买点3触发：✅ 满足5个严格条件
-----------------
买点3建议仓位：70% (60% + 20% 但上限70%)
首次开仓建议：21% (70% × 30%)
计算说明：开仓逻辑60% + 买点3加成20% = 80%，但上限70%
```

## 💻 技术实现

### 代码逻辑
```python
# 获取开仓逻辑建议
opening_logic_data = get_opening_suggestion()
opening_can_long = opening_logic_data.get('can_long', False)
opening_position_percent = opening_logic_data.get('position_info', {}).get('position_percent', 0)

# 买点3仓位计算
if buy_point_3:
    if opening_can_long and opening_position_percent > 0:
        # 情况2：开仓逻辑允许开仓
        bp3_position = min(opening_position_percent + 20, 70)
        suggested_position = f'{int(bp3_position)}%'
        position_calculation_note = f'开仓逻辑{int(opening_position_percent)}% + 买点3加成20% = {int(bp3_position)}% (上限70%)'
    else:
        # 情况1：开仓逻辑不允许开仓
        suggested_position = '20%'
        position_calculation_note = '开仓逻辑不允许，买点3可额外开20%'
```

### API响应示例
```json
{
  "symbol": "BTC-USDT-SWAP",
  "buy_point": "买点3 - 空转多买入",
  "suggested_position": "40%",
  "initial_position": "12%",
  "position_calculation_note": "开仓逻辑20% + 买点3加成20% = 40% (上限70%)",
  "opening_logic_position": "20%"
}
```

## ⚡ 关键要点

1. **买点3是加成型买点**：它在开仓逻辑的基础上额外增加20%
2. **保底机制**：即使开仓逻辑不允许，买点3仍可开20%
3. **上限保护**：无论如何计算，买点3最高只能到70%
4. **首次开仓**：总是建议仓位的30%作为首次开仓
5. **严格筛选**：买点3有5个严格条件，触发机会较少但质量高

## 🔄 实时同步

系统每30秒自动刷新：
- 开仓逻辑的最新建议
- 买点3的触发情况
- 仓位计算结果

确保仓位建议始终基于最新市场状态。

---
**更新时间**: 2025-12-12 13:33
**文档版本**: v1.0
