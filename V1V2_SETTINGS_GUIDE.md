# V1V2 阈值设置功能使用指南

## 📌 功能概述

V1V2阈值设置页面是一个可视化配置界面，允许您实时修改27个币种的V1/V2成交额阈值，无需手动编辑代码或重启服务。

---

## 🌐 访问地址

**设置页面**: https://5000-iypypqmz2wvn9dmtq7ewn-583b4d74.sandbox.novita.ai/v1v2-settings

---

## ✨ 主要功能

### 1. 实时可视化配置
- 📊 **27个币种独立配置**: 每个币种都有自己的V1/V2阈值卡片
- 🎨 **直观的界面**: 现代化深色主题，清晰的视觉层次
- 🔍 **实时搜索**: 快速找到需要修改的币种
- 📈 **即时反馈**: 修改后立即显示修改状态

### 2. 智能验证
- ✅ **自动验证**: 确保V1阈值始终大于V2阈值
- ⚠️ **错误提示**: 配置错误时给出明确提示
- 🔄 **快速重置**: 一键重置所有修改到原始值

### 3. 持久化存储
- 💾 **自动保存**: 配置保存到JSON文件
- 🔄 **动态加载**: 采集器自动检测并应用新配置
- 📝 **配置追踪**: 显示最后保存时间

---

## 📖 使用方法

### 步骤1: 访问设置页面

访问设置页面URL或从主页导航菜单进入：
```
https://5000-iypypqmz2wvn9dmtq7ewn-583b4d74.sandbox.novita.ai/v1v2-settings
```

### 步骤2: 修改阈值

1. **找到要修改的币种**
   - 使用顶部搜索框快速查找
   - 或直接在币种列表中浏览

2. **修改V1/V2阈值**
   - 在对应的输入框中输入新的阈值金额
   - 输入框支持数字键盘，建议使用千位步进
   - 修改后卡片会自动标记为"已修改"状态

3. **查看修改统计**
   - 顶部统计面板实时显示已修改的币种数量
   - 每个修改的卡片会显示橙色边框

### 步骤3: 保存设置

1. **点击"💾 保存设置"按钮**
   - 系统会验证所有配置的有效性
   - 确保所有V1阈值都大于对应的V2阈值

2. **确认保存成功**
   - 收到成功通知消息
   - "最后保存"时间会更新
   - 所有"已修改"标记会清除

3. **采集器自动应用**
   - V1V2采集器会在下一个采集周期（30秒内）自动加载新配置
   - 无需手动重启任何服务

### 步骤4: 验证配置生效

访问监控页面查看新阈值是否生效：
```
https://5000-iypypqmz2wvn9dmtq7ewn-583b4d74.sandbox.novita.ai/v1v2-monitor
```

---

## 🎯 配置示例

### 示例1: 提高BTC的V1阈值

**原始配置**:
- V1: $200,000
- V2: $100,000

**修改为**:
- V1: $300,000 (提高灵敏度)
- V2: $100,000 (保持不变)

**效果**: BTC需要达到30万美元成交额才会显示V1级别

### 示例2: 调整ETH的敏感度

**原始配置**:
- V1: $1,300,000
- V2: $500,000

**修改为**:
- V1: $1,000,000 (降低门槛)
- V2: $400,000 (同步降低)

**效果**: ETH更容易触发V1/V2级别告警

---

## ⚠️ 重要注意事项

### 配置规则
1. **V1 > V2**: V1阈值必须严格大于V2阈值
2. **正数验证**: 所有阈值必须为正数
3. **整数建议**: 建议使用整数，避免小数点

### 最佳实践
1. **批量修改**: 建议一次性修改完所有需要调整的币种，然后统一保存
2. **测试验证**: 修改后观察一段时间，确认新阈值合理
3. **备份记录**: 重要的阈值变更建议记录原因和时间
4. **避免极端值**: 不要设置过高或过低的阈值，可能导致告警失效

---

## 🔧 技术细节

### 配置文件位置
```
/home/user/webapp/v1v2_settings.json
```

### 配置文件格式
```json
{
  "BTC": {"v1": 200000, "v2": 100000},
  "ETH": {"v1": 1300000, "v2": 500000},
  ...
}
```

### 配置加载机制
1. **首次加载**: 采集器启动时从设置文件读取配置
2. **动态更新**: 通过标记文件触发配置重新加载
3. **自动检测**: 每个采集周期检查是否有配置更新

### API接口

#### 获取当前配置
```bash
GET /api/v1v2/settings
```

**响应示例**:
```json
{
  "success": true,
  "settings": {
    "BTC": {"v1": 200000, "v2": 100000},
    ...
  }
}
```

#### 保存新配置
```bash
POST /api/v1v2/settings
Content-Type: application/json

{
  "settings": {
    "BTC": {"v1": 300000, "v2": 100000},
    ...
  }
}
```

**响应示例**:
```json
{
  "success": true,
  "message": "设置已保存，采集器将在下次采集时使用新配置"
}
```

---

## 🚀 快捷操作

### 重置所有修改
点击"🔄 重置全部"按钮，可以将所有未保存的修改恢复到最后保存的状态。

### 搜索币种
使用顶部搜索框输入币种符号（如"BTC"、"ETH"）快速定位。

### 批量调整
可以同时修改多个币种的阈值，最后统一保存。

---

## 🎨 界面说明

### 卡片颜色标识
- **蓝色边框**: 未修改的币种
- **橙色边框**: 已修改但未保存的币种
- **渐变背景**: 深色主题，护眼舒适

### 状态提示
- **已修改徽章**: 显示在修改过的币种卡片上
- **原始值提示**: 在输入框下方显示原始值
- **统计面板**: 实时显示总数和修改数

---

## 📊 系统集成

### 与监控页面联动
设置页面的阈值修改会立即应用到：
- `/v1v2-monitor` - V1V2监控页面
- `/api/v1v2/latest` - V1V2数据API

### 与采集器联动
- `v1v2_collector.py` 会自动检测配置更新
- 下一个采集周期（30秒内）即可应用新配置
- 无需重启任何服务

---

## 💡 使用场景

### 场景1: 市场波动调整
当市场整体成交量增大时，可以适当提高阈值，减少误报。

### 场景2: 重点币种监控
对重要币种降低阈值，提高监控敏感度。

### 场景3: 临时策略调整
快速调整阈值以适应特定交易策略。

### 场景4: 历史数据分析
通过调整阈值观察不同参数下的V1/V2分布。

---

## 🔍 故障排查

### 问题1: 保存失败
**可能原因**: V1阈值小于或等于V2阈值  
**解决方案**: 确保V1 > V2，然后重新保存

### 问题2: 采集器未应用新配置
**可能原因**: 采集器未检测到更新标记  
**解决方案**: 等待下一个采集周期（最多30秒）

### 问题3: 页面无法访问
**可能原因**: Flask服务未运行  
**解决方案**: 
```bash
cd /home/user/webapp
python3 app_new.py &
```

---

## 📝 版本历史

### v1.0 (2025-12-09)
- ✅ 初始版本发布
- ✅ 27个币种独立配置
- ✅ 实时搜索和过滤
- ✅ 自动保存和加载
- ✅ 配置验证功能
- ✅ 响应式设计

---

## 🎉 总结

V1V2阈值设置页面提供了一个友好、直观的配置界面，让您能够轻松管理27个币种的成交额监控阈值。通过这个工具，您可以：

- 🚀 **快速调整**: 无需编辑代码，点击即可修改
- 🔄 **实时生效**: 保存后30秒内自动应用
- 🛡️ **安全可靠**: 自动验证，防止配置错误
- 📊 **可视化**: 清晰直观的界面设计

立即访问设置页面，开始优化您的V1V2监控策略！

---

*文档最后更新: 2025-12-09*  
*作者: GenSpark AI Developer*
