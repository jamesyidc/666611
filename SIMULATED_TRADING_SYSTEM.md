# 模拟交易系统说明

## 系统定位

本系统是一个 **模拟交易系统**，用于测试和验证交易策略，**不执行实际的交易操作**。

---

## 核心架构

```
┌──────────────────────────────────────────────────────────┐
│                   模拟交易系统                           │
│            (Simulated Trading System)                    │
└──────────────────────────────────────────────────────────┘
                         ↓
        ┌────────────────┴────────────────┐
        ↓                                 ↓
┌─────────────────┐              ┌─────────────────┐
│  交易决策引擎   │              │  OKEx 数据源    │
│  (本地计算)     │              │  (仅数据校准)   │
│                 │              │                 │
│ • 开仓规则      │              │ • 实时行情      │
│ • 补仓规则      │              │ • 持仓数据      │
│ • 止盈止损      │              │ • 价格数据      │
│ • 锚点单管理    │              │                 │
└─────────────────┘              └─────────────────┘
        ↓                                 ↓
        └────────────────┬────────────────┘
                         ↓
┌──────────────────────────────────────────────────────────┐
│            trading_decision.db (模拟交易数据库)          │
│                                                          │
│  • position_opens (模拟持仓)                             │
│  • pending_orders (待开仓订单 - 模拟)                    │
│  • add_position_orders (补仓订单 - 模拟)                 │
│  • protect_orders (保护挂单 - 模拟)                      │
│  • *_decision_logs (决策日志)                            │
└──────────────────────────────────────────────────────────┘
```

---

## 数据流向

### 1. OKEx 数据采集（仅用于校准）

```python
# position-sync-fast 守护进程
OKEx API (/api/v5/account/positions)
    ↓
获取实时持仓数据（用于数据校准）
    ↓
同步到 trading_decision.db
    ↓
标记为锚点单（is_anchor=1，如果 margin ≤ 2.0 USDT）
```

**作用**：
- ✅ 获取实时市场数据
- ✅ 校准价格和持仓信息
- ✅ 提供真实的市场环境
- ❌ **不执行任何交易操作**

### 2. 模拟交易决策

```python
# 交易决策引擎
市场数据（来自 OKEx）
    ↓
应用交易规则
    ↓
生成交易决策（开仓/补仓/止盈止损）
    ↓
记录到数据库（模拟执行）
    ↓
更新前端显示
```

**特点**：
- ✅ 所有决策都是模拟的
- ✅ 不会在 OKEx 实际下单
- ✅ 可以安全测试各种策略
- ✅ 记录所有决策过程

---

## 系统功能模块

### 1. 持仓管理（模拟）

**数据表**：`position_opens`

**功能**：
- 显示当前模拟持仓
- 从 OKEx 同步数据用于校准
- 自动识别锚点单
- 计算盈亏（基于实时价格）

**重要**：
- ⚠️ 持仓数据来自 OKEx，但**仅用于显示和校准**
- ⚠️ 任何修改不会影响 OKEx 的实际持仓
- ⚠️ 这些是**观察性持仓**，不是交易指令

### 2. 开仓决策（模拟）

**数据表**：`pending_orders`、`open_decision_logs`

**功能**：
- 根据市场数据和交易规则生成开仓建议
- 记录开仓决策日志
- 显示待开仓订单列表

**重要**：
- ✅ 生成的订单是模拟的
- ✅ 不会在 OKEx 实际下单
- ✅ 用于测试开仓策略的有效性

### 3. 补仓决策（模拟）

**数据表**：`add_position_orders`、`add_decision_logs`

**功能**：
- 根据持仓情况和市场变化生成补仓建议
- 记录补仓决策日志
- 显示补仓订单列表

**重要**：
- ✅ 补仓建议基于实时数据
- ✅ 不会执行实际补仓操作
- ✅ 可以评估补仓策略效果

### 4. 止盈止损（模拟）

**数据表**：`stop_profit_loss_logs`

**功能**：
- 监控持仓盈亏
- 触发止盈止损条件时记录
- 锚点单不参与止盈止损

**重要**：
- ✅ 自动扫描持仓盈亏
- ✅ 记录触发条件
- ❌ **锚点单不执行止盈止损**

### 5. 锚点单管理（模拟）

**数据表**：`position_opens` (is_anchor=1)、`anchor_maintenance_logs`

**功能**：
- 自动识别锚点单（保证金 ≤ 2.0 USDT）
- 监控锚点单状态
- 亏损 ≥ 10% 时触发维护
- 维护策略：买入10倍保证金 + 平仓到1U

**重要**：
- ✅ 锚点单数据来自 OKEx
- ✅ 维护计划是模拟的
- ❌ **不会在 OKEx 执行维护操作**

### 6. 保护性挂单（模拟）

**数据表**：`protect_orders`

**功能**：
- 生成保护性挂单建议
- 防止极端行情损失
- 记录挂单决策

**重要**：
- ✅ 挂单建议基于风险评估
- ✅ 不会在 OKEx 实际挂单
- ✅ 用于测试风险控制策略

---

## 数据同步机制

### position-sync-fast 守护进程

**作用**：从 OKEx 同步持仓数据，用于数据校准

**配置**：
```python
# sync_positions_fast.py
同步间隔：15 秒
数据延迟：2-5 秒
自动识别锚点单：margin ≤ 2.0 USDT
```

**数据流**：
```
OKEx API (每15秒)
    ↓
获取持仓列表
    ↓
更新 position_opens 表
    ↓
标记锚点单 (is_anchor=1)
    ↓
前端显示（30秒自动刷新）
```

**重要说明**：
- ⚠️ 这是**单向数据同步**（OKEx → 数据库）
- ⚠️ 数据库的修改**不会**同步到 OKEx
- ⚠️ 仅用于获取最新的市场数据和价格

---

## 系统配置

### 交易配置（模拟）

**配置文件**：`trading_config.json`

**参数**：
```json
{
  "market_mode": "中性",  // 市场模式
  "margin_usage": "中性",  // 保证金使用
  "total_capital": 1000,  // 模拟总本金 (USDT)
  "usable_capital": 600,  // 模拟可用本金 (USDT)
  "open_capital": 60,     // 单次开仓本金 (USDT)
  "margin_type": "空单保证金", // 保证金类型
  // ... 其他配置
}
```

### 锚点单配置

**保证金范围**：
- 安全范围：**0.15 ~ 1.0 USDT**
- 关注范围：1.0 ~ 2.0 USDT
- 超限：> 2.0 USDT（需要调整）

**维护触发条件**：
- 亏损率：**≥ 10%**

**维护策略（模拟）**：
1. 买入 10 倍原保证金（10x 杠杆 = 100x 价值）
2. 平仓到保留约 1.0 USDT 保证金
3. 记录维护日志

---

## 前端界面

### 主要功能

1. **系统配置** 📊
   - 市场模式设置
   - 保证金配置
   - 本金管理

2. **当前持仓** 🔶
   - 显示所有模拟持仓
   - 锚点单标记
   - 实时盈亏显示

3. **待开仓订单** 📝
   - 开仓建议列表
   - 决策依据
   - 模拟下单

4. **补仓订单** ➕
   - 补仓建议列表
   - 补仓时机
   - 模拟补仓

5. **保护性挂单** 🛡️
   - 挂单建议
   - 风险控制
   - 模拟挂单

6. **决策日志** 📋
   - 开仓日志
   - 补仓日志
   - 止盈止损日志
   - 锚点维护日志

### 数据更新

- **自动刷新**：30 秒
- **手动刷新**：点击"🔄 手动刷新"按钮
- **更新时间**：实时显示

---

## 系统特点

### ✅ 优点

1. **安全性**
   - 不执行实际交易
   - 可以大胆测试策略
   - 零资金风险

2. **真实性**
   - 使用 OKEx 实时数据
   - 价格和行情真实
   - 市场环境真实

3. **完整性**
   - 完整的交易流程
   - 所有决策都有记录
   - 可以回溯分析

4. **灵活性**
   - 可以随时调整策略
   - 可以重置数据
   - 可以对比不同策略

### ⚠️ 限制

1. **不执行交易**
   - 所有操作都是模拟的
   - 不会影响 OKEx 账户
   - 无法获得实际收益

2. **滑点和成交**
   - 不考虑实际滑点
   - 假设订单全部成交
   - 不考虑流动性问题

3. **心理因素**
   - 模拟交易没有心理压力
   - 实盘可能有不同表现
   - 需要考虑情绪管理

---

## 使用场景

### 1. 策略测试
- 测试新的交易策略
- 验证开仓/补仓规则
- 评估风险控制效果

### 2. 数据分析
- 分析历史决策
- 统计策略胜率
- 优化参数设置

### 3. 学习训练
- 熟悉交易流程
- 理解市场规律
- 积累交易经验

### 4. 策略对比
- 对比不同策略效果
- A/B 测试
- 寻找最优策略

---

## 数据说明

### position_opens 表

| 字段 | 说明 | 来源 |
|------|------|------|
| inst_id | 合约代码 | OKEx |
| pos_side | 持仓方向 (long/short) | OKEx |
| open_size | 持仓数量 | OKEx |
| avg_price | 开仓均价 | OKEx |
| mark_price | 标记价格 | OKEx |
| margin | 保证金 | OKEx |
| lever | 杠杆倍数 | OKEx |
| profit_rate | 盈亏率 | 计算 |
| is_anchor | 是否锚点单 | 自动标记 |
| updated_time | 更新时间 | 同步时间 |

**重要**：
- ⚠️ 所有数据来自 OKEx，仅用于显示
- ⚠️ 修改这些数据不会影响 OKEx
- ⚠️ is_anchor 是系统自动标记的

### 其他表（模拟数据）

- `pending_orders` - 模拟待开仓订单
- `add_position_orders` - 模拟补仓订单
- `protect_orders` - 模拟保护挂单
- `*_decision_logs` - 决策日志

---

## 与实盘的区别

| 方面 | 模拟交易 | 实盘交易 |
|------|----------|----------|
| 数据来源 | OKEx API（仅读取） | OKEx API（读写） |
| 订单执行 | 记录到数据库 | 提交到 OKEx |
| 资金变化 | 模拟计算 | 实际扣除 |
| 风险 | 零风险 | 真实风险 |
| 收益 | 模拟收益 | 实际收益 |
| 滑点 | 不考虑 | 真实滑点 |
| 成交 | 假设全部成交 | 可能部分成交 |
| 心理压力 | 无压力 | 有压力 |

---

## 升级到实盘

如果未来需要升级到实盘交易，需要修改以下模块：

### 1. 订单执行模块

```python
# 模拟交易
def place_order_simulated(inst_id, side, size, ...):
    """模拟下单：仅记录到数据库"""
    conn = sqlite3.connect('trading_decision.db')
    c = conn.cursor()
    c.execute('INSERT INTO pending_orders (...) VALUES (...)')
    conn.commit()
    conn.close()

# 实盘交易
def place_order_real(inst_id, side, size, ...):
    """实盘下单：提交到 OKEx"""
    okex_api = OKExAPI()
    response = okex_api.place_order(
        inst_id=inst_id,
        side=side,
        size=size,
        ...
    )
    # 记录订单到数据库
    save_order_to_db(response)
    return response
```

### 2. 持仓同步模块

```python
# 模拟交易：仅同步数据
def sync_positions():
    """从 OKEx 同步持仓（仅读取）"""
    positions = okex_api.get_positions()
    update_database(positions)

# 实盘交易：同步 + 对账
def sync_positions_with_reconciliation():
    """从 OKEx 同步持仓 + 对账"""
    positions = okex_api.get_positions()
    local_positions = get_local_positions()
    reconcile(positions, local_positions)
    update_database(positions)
```

### 3. 风险控制模块

```python
# 模拟交易：宽松控制
risk_check = False

# 实盘交易：严格控制
risk_check = True
max_position_size = 1000  # 最大持仓
max_loss_per_trade = 0.02  # 单笔最大亏损 2%
max_daily_loss = 0.05     # 单日最大亏损 5%
```

---

## 系统监控

### 关键指标

1. **数据同步状态**
   - 同步间隔：15 秒
   - 数据延迟：2-5 秒
   - 同步成功率：> 99%

2. **持仓状态**
   - 总持仓数
   - 锚点单数量
   - 平均盈亏

3. **决策统计**
   - 开仓决策数
   - 补仓决策数
   - 止盈止损触发数
   - 锚点维护触发数

### PM2 进程监控

```bash
pm2 list

关键进程：
- position-sync-fast：持仓同步守护进程
- flask-app：Web 服务器
- anchor-system：锚点单自动管理系统
```

---

## 常见问题

### Q1: 为什么持仓数据会自动更新？
**A**: position-sync-fast 守护进程每 15 秒从 OKEx 同步一次数据，用于数据校准。

### Q2: 修改数据库中的持仓会影响 OKEx 吗？
**A**: 不会。这是单向同步，数据库的修改不会影响 OKEx。

### Q3: 生成的订单会在 OKEx 执行吗？
**A**: 不会。所有订单都是模拟的，不会提交到 OKEx。

### Q4: 如何清空模拟数据？
**A**: 使用 `clear_all_positions.py` 脚本可以清空持仓数据（会自动备份）。

### Q5: 锚点单的保证金为什么这么小？
**A**: 这是 OKEx 的真实持仓数据，用于测试锚点单管理策略。

### Q6: 可以同时运行多个策略吗？
**A**: 可以，但需要注意数据隔离，避免策略之间相互干扰。

---

## 总结

本系统是一个 **完整的模拟交易系统**：

✅ **使用真实数据**：从 OKEx 获取实时行情和价格  
✅ **模拟执行**：所有交易操作都是模拟的  
✅ **零风险**：不会影响 OKEx 账户，不会有资金损失  
✅ **完整流程**：包含开仓、补仓、止盈止损、锚点管理等完整功能  
✅ **数据记录**：所有决策都有日志，可以回溯分析  

**适用场景**：
- 🧪 策略测试
- 📊 数据分析
- 📚 学习训练
- 🔄 策略优化

**下一步**：
- 根据模拟结果优化策略
- 积累足够的测试数据
- 评估策略的稳定性
- 准备升级到实盘（如需要）

---

## 访问系统

**统一管理入口**：  
https://5000-iawcy3xxhnan90u0qd9wq-cc2fbc16.sandbox.novita.ai/trading-manager

**系统状态**：  
✅ 正常运行  
📊 模拟交易模式  
🔄 数据实时同步
