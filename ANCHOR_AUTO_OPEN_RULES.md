# 🚀 锚点单自动开仓规则文档

## 📋 规则总览

本文档定义了锚点单的自动开仓和维护规则，所有规则必须严格遵守，如遇冲突需人工确认。

---

## 🎯 第一部分：新建锚点单规则

### 触发条件（必须全部满足）

#### 1. 逃顶信号存在 ✅
- **数据源**：support_resistance_levels 表
- **条件**：
  - 距离压力线1 ≤ 2%
  - 位置百分比 ≥ 90%（接近顶部）

#### 2. 压力线完整性 ✅
- **必须同时存在**：
  - 压力线1（resistance_line_1）≠ NULL
  - 压力线2（resistance_line_2）≠ NULL
- **逻辑**：两条压力线都必须存在才能开锚点单

#### 3. 没有原始锚点单 ✅
- **检查逻辑**：
  ```sql
  SELECT COUNT(*) FROM position_opens 
  WHERE inst_id = ? 
    AND pos_side = 'short' 
    AND is_anchor = 1
    AND status = 'active'
  ```
- **判断**：如果 COUNT = 0，说明没有原始锚点单，可以新建

### 开仓参数

#### 1. 开仓金额
- **最小单位**：1 USDT
- **动态计算**：
  - 获取币种最新价格
  - 计算最小开仓张数
  - 如果 1 USDT < 最小开仓要求，则使用最小开仓金额（可能是 5 USDT）
  
  ```python
  # 伪代码
  min_contract_value = price * min_size
  if min_contract_value > 1:
      open_amount = min_contract_value
  else:
      open_amount = 1.0
  ```

#### 2. 开仓方向
- **固定**：short（做空）
- **原因**：锚点单是防护性空单，只在逃顶时触发

#### 3. 开仓价格
- **使用**：当前市场价格（current_price）

#### 4. 标记字段
- **is_anchor**：必须设置为 `True`
- **granularity**：设置为 'anchor'
- **trigger_reason**：记录触发原因和压力线信息

---

## 🔧 第二部分：维护锚点单规则

### 触发条件（必须全部满足）

#### 1. 已有原始锚点单 ✅
- **检查逻辑**：
  ```sql
  SELECT * FROM position_opens 
  WHERE inst_id = ? 
    AND pos_side = 'short' 
    AND is_anchor = 1
    AND status = 'active'
  LIMIT 1
  ```
- **判断**：如果存在记录，说明有原始锚点单

#### 2. 再次出现逃顶信号 ✅
- **条件**：与新建锚点单的条件相同
  - 逃顶信号存在
  - 压力线1和压力线2都存在

#### 3. 当前亏损状态 ⚠️
- **检查**：原始锚点单的 profit_rate
- **判断**：
  - 如果 profit_rate < 0：亏损中，可以触发维护
  - 如果 profit_rate ≥ 0：盈利中，**不触发维护**（规则待确认）

### 维护动作

#### 方案A：继续开新的锚点单（补仓）
- **金额**：与原始锚点单相同（1 USDT或动态最小值）
- **标记**：is_anchor = True
- **记录**：记录为补仓操作

#### 方案B：维持原锚点单，不新增
- **逻辑**：只更新监控，不开新仓
- **原因**：避免过度开仓

> ⚠️ **规则冲突点**：
> 
> 当已有锚点单且再次出现逃顶信号时，应该执行哪个方案？
> 
> **请确认**：
> - 方案A：继续开新的锚点单（可能导致该币种有多个锚点单）
> - 方案B：维持原锚点单，只监控不新增
> 
> **当前默认**：方案B（避免过度开仓）

---

## 🛡️ 第三部分：风控规则

### 1. 单币种限制 ✅
- **计算**：
  ```
  可开仓额 = 总本金 × 可开仓百分比
  单币种上限 = 可开仓额 × 单币种最大占比
  ```
- **检查**：
  ```
  当前持仓价值 + 新开仓金额 ≤ 单币种上限
  ```
- **不通过**：拒绝开仓，记录原因

### 2. 系统开关 ✅
- **allow_anchor**：必须为 True
- **enabled**：必须为 True
- **simulation_mode**：
  - True：记录但不实际下单
  - False：实际执行下单

### 3. 重复开仓防护 ✅
- **检查间隔**：同一币种在 5 分钟内不能重复触发
- **实现**：
  ```sql
  SELECT COUNT(*) FROM anchor_triggers
  WHERE inst_id = ?
    AND timestamp > datetime('now', '-5 minutes')
  ```
- **判断**：如果 COUNT > 0，跳过本次触发

### 4. 最小开仓金额 ✅
- **下限**：1 USDT
- **上限**：根据币种价格和最小合约大小动态计算
- **验证**：确保开仓金额 ≥ 交易所最小要求

---

## 📊 第四部分：日志和可视化

### 1. 触发记录表 ✅

```sql
CREATE TABLE IF NOT EXISTS anchor_triggers (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    inst_id TEXT NOT NULL,
    trigger_type TEXT NOT NULL,  -- 'new' 或 'maintain'
    has_existing_anchor INTEGER, -- 是否已有锚点单
    pressure1 REAL NOT NULL,
    pressure2 REAL NOT NULL,
    current_price REAL NOT NULL,
    open_amount REAL NOT NULL,
    action_taken TEXT NOT NULL,  -- 'created' / 'skipped' / 'failed'
    skip_reason TEXT,            -- 跳过原因
    trigger_reason TEXT,
    status TEXT NOT NULL,
    timestamp TEXT NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
)
```

### 2. 操作日志 ✅

每次触发都要记录：
- ✅ 触发时间
- ✅ 币种信息
- ✅ 触发类型（新建/维护）
- ✅ 是否已有锚点单
- ✅ 压力线信息
- ✅ 开仓金额
- ✅ 执行动作
- ✅ 跳过原因（如果跳过）

### 3. Web界面展示 ✅

#### 展示内容：
- 最近20条触发记录
- 触发类型标识
- 是否执行
- 执行结果
- 详细原因

#### 实时监控：
- 当前逃顶信号列表
- 每个币种的锚点单状态
- 触发条件满足情况
- 下一步建议操作

---

## 🔄 第五部分：执行流程

### 1. 自动扫描流程

```
每5分钟执行一次：
├── 步骤1：获取配置（检查系统开关）
├── 步骤2：获取逃顶信号列表
├── 步骤3：遍历每个信号
│   ├── 检查是否有原始锚点单
│   ├── 如果没有 → 执行"新建锚点单"流程
│   ├── 如果有 → 执行"维护锚点单"流程
│   └── 记录触发日志
├── 步骤4：生成报告
└── 步骤5：返回执行结果
```

### 2. 新建锚点单流程

```
新建锚点单：
├── 检查1：系统开关（allow_anchor, enabled）
├── 检查2：逃顶信号存在
├── 检查3：压力线1和压力线2存在
├── 检查4：没有原始锚点单
├── 检查5：单币种限制
├── 检查6：重复触发防护（5分钟）
├── 计算开仓金额（最小1U，动态调整）
├── 如果simulation_mode=true → 记录模拟交易
├── 如果simulation_mode=false → 实际下单
└── 记录触发日志
```

### 3. 维护锚点单流程

```
维护锚点单：
├── 检查1：原始锚点单存在
├── 检查2：逃顶信号再次出现
├── 检查3：当前亏损状态（profit_rate < 0）
├── 判断：执行方案A还是方案B？
│   ├── 方案A：开新锚点单（需确认）
│   │   ├── 检查单币种限制
│   │   ├── 检查重复触发防护
│   │   └── 执行开仓
│   └── 方案B：仅监控（当前默认）
│       └── 记录监控日志
└── 记录触发日志
```

---

## ⚠️ 第六部分：需要确认的规则冲突

### 冲突1：维护锚点单的执行方案

**问题**：当已有锚点单且再次出现逃顶信号时，应该：
- **方案A**：继续开新的锚点单（补仓）
- **方案B**：维持原锚点单，只监控不新增

**当前默认**：方案B

**请确认您的选择**：[ ] 方案A  [ ] 方案B

---

### 冲突2：盈利状态下的维护

**问题**：如果原始锚点单处于盈利状态（profit_rate ≥ 0），是否触发维护？
- **选项A**：不触发，只在亏损时维护
- **选项B**：无论盈亏，只要有逃顶信号就维护

**当前默认**：选项A

**请确认您的选择**：[ ] 选项A  [ ] 选项B

---

### 冲突3：最小开仓金额的处理

**问题**：如果币种价格高，1 USDT 无法满足最小开仓要求：
- **选项A**：动态调整到最小可开仓金额（可能5-10 USDT）
- **选项B**：跳过该币种，不开仓

**当前默认**：选项A

**请确认您的选择**：[ ] 选项A  [ ] 选项B

---

## 📈 第七部分：测试和验证

### 1. 单元测试

- [ ] 测试逃顶信号获取
- [ ] 测试压力线完整性检查
- [ ] 测试原始锚点单检查
- [ ] 测试开仓金额计算
- [ ] 测试单币种限制
- [ ] 测试重复触发防护
- [ ] 测试维护锚点单逻辑

### 2. 集成测试

- [ ] 测试完整的新建流程
- [ ] 测试完整的维护流程
- [ ] 测试模拟模式
- [ ] 测试实盘模式
- [ ] 测试日志记录
- [ ] 测试Web界面展示

### 3. 压力测试

- [ ] 测试多个币种同时触发
- [ ] 测试频繁触发场景
- [ ] 测试异常情况处理

---

## 📞 联系和反馈

如有任何疑问或需要澄清的规则，请及时反馈！

**重要**：在实现前，请务必确认以下冲突点：
1. 维护锚点单的执行方案（方案A或B）
2. 盈利状态下是否维护（选项A或B）
3. 最小开仓金额处理（选项A或B）

---

**文档版本**：v1.0  
**创建时间**：2025-12-28  
**状态**：待确认冲突规则
