# 止盈止损系统规则文档

## 📋 文档信息
- **创建时间**: 2025-12-28
- **版本**: v1.0
- **状态**: 待确认
- **作者**: AI Assistant + User Requirements

---

## 🎯 核心功能概述

止盈止损系统是交易管理系统的核心风控模块，负责：
1. 监控所有持仓的盈亏状态
2. 根据预设规则自动触发止盈/止损操作
3. 记录完整的决策过程和执行日志
4. 提供可视化的决策追溯

---

## 📊 止盈止损类型

### 1. 止盈 (Take Profit)
触发条件：持仓盈利达到预设目标

### 2. 止损 (Stop Loss)
触发条件：持仓亏损达到预设止损线

### 3. 移动止损 (Trailing Stop)
触发条件：价格回撤达到设定比例

---

## ⚙️ 规则配置

### 基础配置项

```yaml
止盈配置:
  默认止盈比例: ? %  # 🔴 待确认1：默认止盈比例是多少？
  是否启用移动止盈: ?  # 🔴 待确认2：是否需要移动止盈功能？
  部分止盈比例: ?  # 🔴 待确认3：触发止盈后平仓比例是多少？（如95%）
  
止损配置:
  默认止损比例: ? %  # 🔴 待确认4：默认止损比例是多少？
  是否启用移动止损: ?  # 🔴 待确认5：是否需要移动止损功能？
  全仓止损比例: ?  # 🔴 待确认6：触发止损后平仓比例是多少？（全平还是部分）
  
监控频率:
  检查间隔: ? 秒  # 🔴 待确认7：多久检查一次止盈止损条件？
  
应用范围:
  是否包含锚点单: ?  # 🔴 待确认8：锚点单是否也需要止盈止损？
  是否包含挂单: 否  # 挂单本身就是止盈机制
```

---

## 🔍 触发逻辑

### 止盈触发流程

```
1. 获取所有持仓（排除锚点单？待确认）
2. 计算每个持仓的当前盈亏率
3. 判断是否达到止盈条件
4. 记录决策日志（包含详细判断过程）
5. 执行平仓操作
6. 记录执行结果
```

### 止损触发流程

```
1. 获取所有持仓（排除锚点单？待确认）
2. 计算每个持仓的当前盈亏率
3. 判断是否达到止损条件
4. 记录决策日志（包含详细判断过程）
5. 执行平仓操作
6. 记录执行结果
```

---

## 📝 决策日志内容

每条止盈/止损决策日志应包含：

```json
{
  "log_id": "唯一ID",
  "timestamp": "决策时间",
  "inst_id": "币种",
  "pos_side": "多/空",
  "action_type": "止盈/止损",
  "trigger_info": {
    "open_price": "开仓价格",
    "current_price": "当前价格",
    "profit_rate": "盈亏率",
    "position_size": "持仓大小",
    "position_value": "持仓价值"
  },
  "decision_steps": [
    "步骤1：检查是否为锚点单",
    "步骤2：计算当前盈亏率",
    "步骤3：判断是否达到止盈/止损条件",
    "步骤4：检查系统配置",
    "步骤5：确认可执行"
  ],
  "decision_result": "执行/跳过",
  "execution_info": {
    "close_percent": "平仓比例",
    "close_size": "平仓数量",
    "estimated_profit": "预估盈亏"
  },
  "reason": "详细原因说明"
}
```

---

## 🚦 待确认的规则冲突

### 冲突1：止盈比例设置
- **选项A**: 固定比例（如统一10%止盈）
- **选项B**: 分级比例（如开仓单15%，补仓单10%）
- **选项C**: 动态比例（根据市场波动率调整）
- **当前状态**: ⚠️ 待用户确认

### 冲突2：止损比例设置
- **选项A**: 固定比例（如统一-5%止损）
- **选项B**: 分级比例（如开仓单-5%，补仓单-3%）
- **选项C**: 不设止损（让补仓机制处理）
- **当前状态**: ⚠️ 待用户确认

### 冲突3：锚点单的止盈止损
- **选项A**: 锚点单也参与止盈止损
- **选项B**: 锚点单不参与止盈止损（有专门的挂单保护）
- **当前状态**: ⚠️ 待用户确认

### 冲突4：平仓比例
- **选项A**: 止盈时平掉95%（保留5%继续持仓）
- **选项B**: 止盈时全部平仓（100%）
- **选项C**: 不同情况不同比例
- **当前状态**: ⚠️ 待用户确认

### 冲突5：移动止盈/止损
- **选项A**: 启用移动止盈止损（价格有利时动态调整）
- **选项B**: 不启用（使用固定止盈止损线）
- **当前状态**: ⚠️ 待用户确认

### 冲突6：补仓后的止盈止损
- **选项A**: 补仓后重新计算止盈止损点位
- **选项B**: 补仓后保持原有止盈止损点位
- **选项C**: 补仓后使用加权平均价计算
- **当前状态**: ⚠️ 待用户确认

### 冲突7：模拟模式下的止盈止损
- **选项A**: 模拟模式下真实执行平仓（记录到数据库）
- **选项B**: 模拟模式下仅记录日志不执行
- **当前状态**: ⚠️ 待用户确认

---

## 📊 数据库表结构

### stop_profit_loss_logs 表
用于记录止盈止损的决策和执行日志

```sql
CREATE TABLE IF NOT EXISTS stop_profit_loss_logs (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    inst_id TEXT NOT NULL,
    pos_side TEXT NOT NULL,
    action_type TEXT NOT NULL,  -- 'take_profit' / 'stop_loss'
    open_price REAL,
    current_price REAL,
    profit_rate REAL,
    position_size REAL,
    position_value REAL,
    close_percent REAL,
    close_size REAL,
    estimated_profit REAL,
    decision_steps TEXT,  -- JSON格式
    decision_result TEXT,  -- 'executed' / 'skipped'
    reason TEXT,
    timestamp TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

---

## 🎯 使用场景示例

### 场景1：普通开仓单达到止盈
```
持仓: BTC-USDT-SWAP，开仓价 50000，当前价 55000
盈亏率: +10%
触发条件: 达到止盈目标10%
执行动作: 平仓95%，保留5%
日志记录: 完整决策过程
```

### 场景2：补仓单触发止损
```
持仓: ETH-USDT-SWAP，平均成本 3000，当前价 2850
盈亏率: -5%
触发条件: 达到止损线-5%
执行动作: 全部平仓
日志记录: 完整决策过程
```

---

## 🔧 API接口设计

### 1. 获取止盈止损记录
```http
GET /api/trading/stop-profit-loss/logs?limit=50
```

### 2. 手动触发止盈止损检查
```http
POST /api/trading/stop-profit-loss/scan
```

### 3. 获取当前止盈止损配置
```http
GET /api/trading/stop-profit-loss/config
```

---

## 📈 前端展示要求

### 止盈止损标签页内容

1. **统计卡片区域**
   - 今日止盈次数
   - 今日止损次数
   - 累计止盈金额
   - 累计止损金额

2. **决策日志区域**
   - 实时显示决策过程
   - 每条日志包含：币种、动作类型、盈亏率、执行结果、详细原因
   - 支持筛选（止盈/止损）
   - 支持刷新

3. **执行记录表格**
   - 时间
   - 币种
   - 方向
   - 动作类型
   - 开仓价
   - 平仓价
   - 盈亏率
   - 平仓数量
   - 实际盈亏

---

## 🚀 实施计划

### 阶段1：规则确认（当前）
- ✅ 创建规则文档
- ⏳ 用户确认所有冲突点
- ⏳ 最终确定配置参数

### 阶段2：后端开发
- ⏳ 创建数据库表
- ⏳ 实现止盈止损检查逻辑
- ⏳ 实现决策日志记录
- ⏳ 创建API接口

### 阶段3：前端开发
- ⏳ 添加止盈止损标签页
- ⏳ 实现决策日志展示
- ⏳ 实现执行记录表格
- ⏳ 添加手动触发按钮

### 阶段4：测试验证
- ⏳ 单元测试
- ⏳ 集成测试
- ⏳ 模拟环境验证

---

## ⚠️ 重要提醒

1. **所有规则冲突必须先由用户确认后才能实施**
2. **决策日志必须记录完整的判断过程**
3. **必须区分模拟模式和实盘模式**
4. **锚点单的止盈止损规则需要特别确认**
5. **与挂单系统的协调需要明确**

---

## 📞 待确认问题清单

请用户确认以下问题（按优先级排序）：

### 高优先级
1. ✅ 默认止盈比例是多少？
2. ✅ 默认止损比例是多少？
3. ✅ 锚点单是否参与止盈止损？
4. ✅ 触发止盈后平仓比例是多少？
5. ✅ 触发止损后是全平还是部分平仓？

### 中优先级
6. ✅ 是否需要移动止盈止损功能？
7. ✅ 补仓后如何计算止盈止损点位？
8. ✅ 检查间隔时间是多少？

### 低优先级
9. ✅ 是否需要分级止盈止损（开仓单和补仓单不同）？
10. ✅ 模拟模式下是真实执行还是仅记录日志？

---

**请用户逐一确认以上问题，确认完成后开始实施！** 🚀
