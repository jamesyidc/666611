# 计次得分显示修复报告

## 🔍 问题描述

用户在查询页面 https://5000-ilsitop6yown44mau7vd7-c07dda5e.sandbox.novita.ai/query 发现**计次得分显示为 "---"**（空值）。

![问题截图](https://www.genspark.ai/api/files/s/SXLsnOiW)

---

## 🐛 问题根因

### 1. 两套不同的计算逻辑

系统中存在两个计次得分计算脚本，使用了**不同的逻辑**：

#### `collect_and_store.py`（采集时使用）
```python
# 18-24点时间段
else:  # 18 <= current_hour < 24
    if count_times <= 4:
        return (3, "实心", "★★★")
    elif 4 < count_times <= 5:
        return (2, "实心", "★★☆")
    elif 5 < count_times <= 6:
        return (1, "实心", "★☆☆")
    elif 6 < count_times <= 7:
        return (1, "空心", "☆☆☆")
    elif 7 < count_times <= 8:
        return (2, "空心", "☆☆---")
    else:  # count_times > 8
        return (3, "空心", "☆---")  # count=9 应该返回这个
```

#### `calculate_count_score.py`（旧版本 - 问题代码）
```python
# 18-24点时间段
else:  # 18-24点（24点前）
    if count <= 4:
        return "★★★", "实心3星"
    elif 4 < count <= 5:
        return "★★", "实心2星"
    elif 5 < count <= 6:
        return "★", "实心1星"
    elif 6 < count <= 9:  # 6-9跳过 ❌ 问题在这里！
        return "", "无评分"  # count=9返回空字符串
    elif 9 < count <= 10:
        return "☆", "空心1星"
    else:  # count > 11
        return "☆☆☆", "空心3星"
```

### 2. 数据库中的表现

```
最近10条数据（修复前）:
====================================================================================================
时间                   计次         计次得分显示               得分类型            状态        
====================================================================================================
2025-12-22 20:39:00  9                                 无评分             震荡无序      
2025-12-22 20:29:00  9                                 无评分             震荡无序      
2025-12-22 20:19:00  9                                 无评分             震荡无序      
```

- `count_score_display` 字段为**空字符串**
- `count_score_type` 显示为**"无评分"**

---

## ✅ 修复方案

### 修改 `calculate_count_score.py` 使其与 `collect_and_store.py` 逻辑一致

#### 修复后的逻辑（18-24点时间段）：

```python
else:  # 18-24点（24点前）
    if count <= 4:
        return "★★★", "实心3星"
    elif 4 < count <= 5:
        return "★★☆", "实心2星"
    elif 5 < count <= 6:
        return "★☆☆", "实心1星"
    elif 6 < count <= 7:
        return "☆☆☆", "空心1星"
    elif 7 < count <= 8:
        return "☆☆---", "空心2星"
    else:  # count > 8
        return "☆---", "空心3星"  # ✅ count=9 正确返回
```

### 完整规则对照表

| 时间段 | count范围 | 显示 | 类型 |
|-------|----------|------|------|
| **00-06点** | count <= 1 | ★★★ | 实心3星 |
| | 1 < count <= 2 | ★★☆ | 实心2星 |
| | 2 < count <= 3 | ★☆☆ | 实心1星 |
| | 3 < count <= 4 | ☆☆☆ | 空心1星 |
| | 4 < count <= 5 | ☆☆--- | 空心2星 |
| | count > 5 | ☆--- | 空心3星 |
| **06-12点** | count <= 2 | ★★★ | 实心3星 |
| | 2 < count <= 3 | ★★☆ | 实心2星 |
| | 3 < count <= 4 | ★☆☆ | 实心1星 |
| | 4 < count <= 5 | ☆☆☆ | 空心1星 |
| | 5 < count <= 6 | ☆☆--- | 空心2星 |
| | count > 6 | ☆--- | 空心3星 |
| **12-18点** | count <= 3 | ★★★ | 实心3星 |
| | 3 < count <= 4 | ★★☆ | 实心2星 |
| | 4 < count <= 5 | ★☆☆ | 实心1星 |
| | 5 < count <= 6 | ☆☆☆ | 空心1星 |
| | 6 < count <= 7 | ☆☆--- | 空心2星 |
| | count > 7 | ☆--- | 空心3星 |
| **18-24点** | count <= 4 | ★★★ | 实心3星 |
| | 4 < count <= 5 | ★★☆ | 实心2星 |
| | 5 < count <= 6 | ★☆☆ | 实心1星 |
| | 6 < count <= 7 | ☆☆☆ | 空心1星 |
| | 7 < count <= 8 | ☆☆--- | 空心2星 |
| | **count > 8** | **☆---** | **空心3星** |

---

## 🔧 执行修复

### 1. 修改代码
```bash
# 编辑 calculate_count_score.py
# 统一所有时间段的计算逻辑
```

### 2. 更新历史数据
```bash
cd /home/user/webapp
python3 calculate_count_score.py
```

**执行结果：**
```
找到 1048 条记录需要更新
已更新 100 条记录...
已更新 200 条记录...
...
已更新 1000 条记录...

更新完成!
成功更新: 1048 条
失败: 0 条
```

---

## ✅ 修复验证

### 修复后的数据：

```
最近10条数据（修复后）:
====================================================================================================
时间                   计次         计次得分显示               得分类型            状态        
====================================================================================================
2025-12-22 20:39:00  9          ☆---                 空心3星            震荡无序      
2025-12-22 20:29:00  9          ☆---                 空心3星            震荡无序      
2025-12-22 20:19:00  9          ☆---                 空心3星            震荡无序      
2025-12-22 20:09:00  9          ☆---                 空心3星            震荡无序      
2025-12-22 19:59:00  9          ☆---                 空心3星            震荡无序      
2025-12-22 19:49:00  9          ☆---                 空心3星            震荡无序      
2025-12-22 19:39:00  9          ☆---                 空心3星            震荡无序      
2025-12-22 19:29:00  9          ☆---                 空心3星            震荡无序      
2025-12-22 19:19:00  9          ☆---                 空心3星            震荡无序      
2025-12-22 19:09:00  8          ☆☆---                空心2星            震荡无序      
```

### API验证：

查询 `/api/query/latest` 接口，返回：
```json
{
  "success": true,
  "data": {
    "运算时间": "2025-12-22 20:39:00",
    "计次": 9,
    "计次得分": "☆---",  // ✅ 已修复
    ...
  }
}
```

### 前端页面验证：

访问 https://5000-ilsitop6yown44mau7vd7-c07dda5e.sandbox.novita.ai/query

**修复前：** 计次得分 = `---`  
**修复后：** 计次得分 = `☆---` ✅

---

## 📊 修复影响范围

- ✅ **修复文件**: `calculate_count_score.py`
- ✅ **更新记录数**: 1048条
- ✅ **影响时间段**: 所有历史数据
- ✅ **Git提交**: a720f5a

---

## 🎯 根本原因总结

1. **代码不一致**: 两个脚本使用不同的计算逻辑
2. **缺少"无评分"逻辑**: `calculate_count_score.py`引入了"跳过/无评分"概念，但`collect_and_store.py`没有
3. **测试不充分**: 没有验证不同时间段、不同count值的组合

---

## 📝 后续建议

### 1. 统一代码逻辑
- ✅ 将计次得分计算函数提取为独立模块
- ✅ 所有脚本使用同一个计算函数
- ✅ 避免逻辑重复

### 2. 添加单元测试
```python
def test_count_score():
    # 测试18-24点，count=9的情况
    assert calculate_count_score("2025-12-22 20:39:00", 9) == ("☆---", "空心3星")
    
    # 测试其他边界情况
    assert calculate_count_score("2025-12-22 20:00:00", 8) == ("☆☆---", "空心2星")
    assert calculate_count_score("2025-12-22 20:00:00", 10) == ("☆---", "空心3星")
```

### 3. 定期验证
- 添加数据质量监控
- 检查是否有空字符串或"无评分"的记录
- 设置告警机制

---

## ✅ 修复完成

- [x] 问题定位：两套不同的计算逻辑
- [x] 代码修复：统一逻辑，移除"无评分"
- [x] 数据更新：更新所有1048条历史记录
- [x] 验证通过：计次得分正确显示`☆---`
- [x] 文档记录：创建修复报告

**修复状态**: ✅ 完成  
**修复时间**: 2025-12-22 20:50  
**Git提交**: a720f5a  
**影响记录**: 1048条

---

## 🔗 相关文件

- `/home/user/webapp/calculate_count_score.py` - 修复的主要文件
- `/home/user/webapp/collect_and_store.py` - 参考的正确逻辑
- `/home/user/webapp/app_new.py` - API查询接口（第8922行）
- `/home/user/webapp/crypto_data.db` - 数据库（crypto_snapshots表）

---

**问题已完全解决！** 🎉
