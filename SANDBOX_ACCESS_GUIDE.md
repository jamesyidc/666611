# 🔗 沙箱访问指南

## 📍 当前状态

### ✅ 服务正常运行

```
✓ Flask服务: 正常 (端口5000)
✓ 深色主题: 已部署
✓ 所有功能: 可用
```

---

## 🌐 访问地址

### 方法1: 直接访问 (推荐)

**深色主题首页**:
```
https://5000-iik759kgm7i3zklxvfrfx-cc2fbc16.sandbox.novita.ai/
```

### 方法2: 通过GetServiceUrl获取

如果上面的URL无法访问，沙箱可能已重启，需要获取新的URL。

---

## ⚠️ "Sandbox Not Found" 错误解决

### 原因分析

这个错误通常由以下原因引起：

1. **沙箱ID变化**: 沙箱重启后ID可能改变
2. **网络缓存**: 浏览器缓存了旧的沙箱信息
3. **临时不可达**: 沙箱服务临时不可访问
4. **URL错误**: 沙箱ID拼写错误

### 解决方案

#### 方案1: 刷新页面 ⭐
```
Windows/Linux: Ctrl + F5
Mac: Cmd + Shift + R
```

#### 方案2: 清除浏览器缓存
1. Chrome: `chrome://settings/clearBrowserData`
2. 勾选 "缓存的图像和文件"
3. 点击 "清除数据"
4. 刷新页面

#### 方案3: 等待重试
- 等待 30-60 秒
- 重新访问链接
- 如果持续失败，可能需要重新部署

#### 方案4: 获取新的沙箱URL
运行以下命令获取最新URL：
```bash
cd /home/user/webapp
python3 -c "from GetServiceUrl import *; print(GetServiceUrl(5000))"
```

---

## 🎨 深色主题特性

### 已实现功能

| 特性 | 状态 |
|-----|------|
| 深色背景 (#1a1a2e → #16213e) | ✅ |
| 深灰卡片 (rgba(42,45,71,0.95)) | ✅ |
| 科技蓝按钮 (#00d4ff) | ✅ |
| 白色文字 (#ffffff) | ✅ |
| 响应式设计 | ✅ |

### 页面路由

```
/           - 首页导航
/query      - 历史数据查询
/chart      - 趋势图表
/timeline   - 时间轴
/status     - 系统状态
```

### API接口

```
/api/stats     - 统计数据
/api/latest    - 最新数据
/api/chart     - 图表数据
/api/timeline  - 时间轴数据
```

---

## 🛠️ 本地测试

### 测试服务状态

```bash
cd /home/user/webapp

# 检查Flask进程
ps aux | grep app_new.py

# 测试服务
curl http://localhost:5000/

# 测试API
curl http://localhost:5000/api/stats
```

### 验证深色主题

```bash
# 运行验证脚本
python3 verify_dark_theme.py
```

---

## 📊 系统信息

### 服务配置

- **主机**: e2b.local
- **内部IP**: 169.254.0.21
- **端口**: 5000
- **协议**: HTTP
- **框架**: Flask

### 数据库状态

```bash
# 查看数据统计
sqlite3 crypto_data.db "SELECT COUNT(*) FROM crypto_snapshots"
```

---

## 🔧 重启服务

### 如需重启Flask

```bash
cd /home/user/webapp

# 停止旧进程
pkill -9 -f "python.*app_new.py"

# 启动新进程
nohup python3 app_new.py > flask.log 2>&1 &

# 等待3秒
sleep 3

# 测试
curl -I http://localhost:5000/
```

---

## 📝 注意事项

### 沙箱环境特性

1. **临时性**: 沙箱可能随时重启
2. **ID变化**: 重启后URL会改变
3. **数据持久**: 数据库文件会保留
4. **进程管理**: 服务需要手动重启

### 最佳实践

1. **保存URL**: 成功访问后保存书签
2. **定期检查**: 如果无法访问，先刷新页面
3. **备份重要数据**: 定期备份数据库
4. **监控服务**: 使用守护进程保持服务运行

---

## 🎯 快速命令

### 一键验证

```bash
cd /home/user/webapp && python3 verify_dark_theme.py
```

### 一键重启

```bash
cd /home/user/webapp && pkill -9 -f app_new.py && sleep 2 && nohup python3 app_new.py > flask.log 2>&1 & && sleep 3 && curl -I http://localhost:5000/
```

### 查看日志

```bash
cd /home/user/webapp && tail -f flask.log
```

---

## 🔗 相关链接

- **GitHub PR**: https://github.com/jamesyidc/6666/pull/1
- **最新提交**: `e12288d`
- **分支**: `genspark_ai_developer`

---

## ✅ 确认清单

- [ ] Flask服务运行中
- [ ] 深色主题已应用
- [ ] 所有路由正常
- [ ] API接口可访问
- [ ] 数据库有数据
- [ ] 守护进程运行中

---

**最后更新**: 2025-12-06 18:45  
**状态**: 服务运行中，深色主题已部署
