# 挂单系统规则文档

## 📋 文档信息
- **创建时间**: 2025-12-28
- **版本**: v1.0
- **状态**: 待确认
- **作者**: AI Assistant + User Requirements

---

## 🎯 核心功能概述

挂单系统是为**锚点单服务的保护机制**，主要目的是：
1. 防止已开锚点单的币种突然急速拉升
2. 在价格上涨到预设位置时自动止盈
3. 分层设置不同的止盈点位和倍数
4. 触发后立即平掉95%的仓位

---

## 🔑 核心规则（已明确）

### 1. 挂单服务对象
- **仅为锚点单服务**
- 普通开仓单不设置挂单
- 只有已经开了锚点单的币种才会设置挂单

### 2. 挂单触发条件
```
条件: 已经存在该币种的锚点单
目的: 防止急速拉升导致锚点单亏损扩大
机制: 在币价上方预设两道防线
```

### 3. 挂单配置（已明确）

#### 第一道防线
```yaml
位置: 当前币价上方 4%
倍数: 10倍
说明: 价格上涨4%时，以10倍杠杆开多单
```

#### 第二道防线
```yaml
位置: 当前币价上方 10%
倍数: 20倍
说明: 价格上涨10%时，以20倍杠杆开多单
```

### 4. 触发后执行动作（已明确）
```
触发挂单后:
1. 立即执行开多单操作
2. 开单成功后立即平掉95%的仓位
3. 保留5%继续持仓
4. 记录完整的触发和执行日志
```

---

## 🔍 待确认的规则冲突

### 冲突1：挂单基准价格
当前币价的确定方式：

- **选项A**: 使用锚点单开仓时的价格
  - 优点：固定不变，计算简单
  - 缺点：如果价格已经下跌，可能永远触发不到

- **选项B**: 使用当前实时价格
  - 优点：动态调整，更灵活
  - 缺点：价格波动时挂单位置会变化

- **选项C**: 使用锚点单的成本价（含补仓后的平均价）
  - 优点：更准确反映实际成本
  - 缺点：补仓后需要重新计算

**当前状态**: ⚠️ 待用户确认

---

### 冲突2：挂单创建时机

- **选项A**: 锚点单开仓的同时立即创建挂单
  - 优点：时机最早，保护最快
  - 缺点：可能频繁创建和取消

- **选项B**: 锚点单开仓后价格下跌一定比例再创建
  - 优点：避免频繁创建
  - 缺点：可能错过急速拉升

- **选项C**: 手动触发创建挂单
  - 优点：完全可控
  - 缺点：可能来不及

**当前状态**: ⚠️ 待用户确认

---

### 冲突3：挂单更新策略

当锚点单补仓后：

- **选项A**: 保持原有挂单不变
  - 优点：简单，不需要重新计算
  - 缺点：可能不准确

- **选项B**: 根据新的平均成本重新设置挂单
  - 优点：更准确
  - 缺点：需要取消旧挂单，创建新挂单

- **选项C**: 增加额外的挂单（不取消旧的）
  - 优点：多重保护
  - 缺点：可能触发多次

**当前状态**: ⚠️ 待用户确认

---

### 冲突4：挂单金额计算

挂单的开仓金额如何确定：

- **选项A**: 基于锚点单的当前总金额
  ```
  例如：锚点单当前总额100 USDT
  第一挂单(10倍)：100 * 10 = 1000 USDT
  第二挂单(20倍)：100 * 20 = 2000 USDT
  ```

- **选项B**: 基于锚点单的初始金额
  ```
  例如：锚点单初始金额10 USDT（补仓后已变成100 USDT）
  第一挂单(10倍)：10 * 10 = 100 USDT
  第二挂单(20倍)：10 * 20 = 200 USDT
  ```

- **选项C**: 固定金额
  ```
  第一挂单：固定100 USDT
  第二挂单：固定200 USDT
  ```

**当前状态**: ⚠️ 待用户确认

---

### 冲突5：挂单触发后的锚点单处理

挂单触发并平仓95%后，原有的锚点单如何处理：

- **选项A**: 保持不变，继续持有
  - 优点：维持原有策略
  - 缺点：已经盈利的情况下还持有空单

- **选项B**: 同时关闭或减仓锚点单
  - 优点：锁定盈利
  - 缺点：可能错过后续下跌机会

- **选项C**: 根据盈亏情况决定
  ```
  如果锚点单盈利：保持不变
  如果锚点单亏损：考虑平仓
  ```

**当前状态**: ⚠️ 待用户确认

---

### 冲突6：挂单数量限制

一个锚点单最多设置多少个挂单：

- **选项A**: 固定两个（4%和10%）
  - 优点：简单明确
  - 缺点：保护层级固定

- **选项B**: 可配置多个（如4%、7%、10%、15%）
  - 优点：更灵活的保护
  - 缺点：复杂度增加

**当前状态**: ⚠️ 待用户确认（从需求看应该是选项A）

---

### 冲突7：挂单取消条件

什么情况下取消挂单：

- **选项A**: 锚点单平仓时自动取消所有挂单
  - 优点：逻辑清晰
  - 缺点：需要监控锚点单状态

- **选项B**: 手动取消
  - 优点：完全可控
  - 缺点：可能忘记取消

- **选项C**: 设置有效期，超时自动取消
  - 优点：自动清理
  - 缺点：可能过早取消

**当前状态**: ⚠️ 待用户确认

---

## 📝 决策日志内容

每条挂单决策日志应包含：

```json
{
  "log_id": "唯一ID",
  "timestamp": "决策时间",
  "inst_id": "币种",
  "action": "create_pending / trigger_pending / cancel_pending",
  "anchor_info": {
    "anchor_id": "关联的锚点单ID",
    "anchor_size": "锚点单金额",
    "anchor_price": "锚点单开仓价",
    "current_anchor_profit": "锚点单当前盈亏"
  },
  "pending_order_info": {
    "level": "第一道/第二道防线",
    "trigger_price": "触发价格（基准价+4%或+10%）",
    "base_price": "基准价格",
    "multiple": "杠杆倍数（10或20）",
    "order_size": "挂单金额"
  },
  "decision_steps": [
    "步骤1：检查是否存在锚点单",
    "步骤2：获取基准价格",
    "步骤3：计算挂单价格（+4%和+10%）",
    "步骤4：计算挂单金额（10倍和20倍）",
    "步骤5：检查资金是否充足",
    "步骤6：确认可创建挂单"
  ],
  "decision_result": "创建成功/触发执行/取消",
  "execution_info": {
    "order_id": "订单ID",
    "trigger_time": "触发时间",
    "actual_price": "实际成交价",
    "close_percent": 95,
    "close_size": "平仓数量",
    "actual_profit": "实际盈亏"
  },
  "reason": "详细原因说明"
}
```

---

## 🎯 使用场景示例

### 场景1：创建挂单
```
前提: BTC-USDT-SWAP已有锚点单，开仓价50000，金额10 USDT
触发: 锚点单创建成功
动作: 
  - 第一挂单：价格52000（+4%），金额100 USDT（10倍）
  - 第二挂单：价格55000（+10%），金额200 USDT（20倍）
日志: 记录完整创建过程
```

### 场景2：挂单触发
```
前提: BTC-USDT-SWAP已有挂单，触发价52000
当前: 价格达到52000
动作:
  1. 开多单，金额100 USDT
  2. 立即平掉95%（95 USDT）
  3. 保留5%（5 USDT）继续持仓
日志: 记录触发过程和执行结果
```

### 场景3：锚点单补仓后更新挂单
```
前提: 锚点单补仓后，总金额从10 USDT变成110 USDT
选项: 根据冲突3确定是否更新挂单
日志: 记录更新决策过程
```

---

## 📊 数据库表结构

### pending_orders 表
存储挂单信息

```sql
CREATE TABLE IF NOT EXISTS pending_orders (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    inst_id TEXT NOT NULL,
    anchor_id INTEGER,  -- 关联的锚点单ID
    level INTEGER NOT NULL,  -- 1: 第一道防线(4%), 2: 第二道防线(10%)
    base_price REAL NOT NULL,  -- 基准价格
    trigger_price REAL NOT NULL,  -- 触发价格
    trigger_percent REAL NOT NULL,  -- 触发百分比(4或10)
    multiple INTEGER NOT NULL,  -- 杠杆倍数(10或20)
    order_size REAL NOT NULL,  -- 挂单金额
    status TEXT DEFAULT 'pending',  -- pending/triggered/cancelled
    order_id TEXT,  -- OKX订单ID
    trigger_time TEXT,  -- 触发时间
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### pending_order_logs 表
存储挂单决策日志

```sql
CREATE TABLE IF NOT EXISTS pending_order_logs (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    inst_id TEXT NOT NULL,
    action TEXT NOT NULL,  -- create/trigger/cancel/update
    anchor_id INTEGER,
    pending_order_id INTEGER,
    decision_steps TEXT,  -- JSON格式
    decision_result TEXT,
    reason TEXT,
    execution_info TEXT,  -- JSON格式
    timestamp TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

---

## 🔧 API接口设计

### 1. 获取挂单记录
```http
GET /api/trading/pending-orders?status=pending&limit=50
```

### 2. 获取挂单决策日志
```http
GET /api/trading/pending-orders/logs?limit=50
```

### 3. 手动创建挂单
```http
POST /api/trading/pending-orders/create
{
  "inst_id": "BTC-USDT-SWAP",
  "anchor_id": 123
}
```

### 4. 取消挂单
```http
POST /api/trading/pending-orders/cancel
{
  "pending_order_id": 456
}
```

---

## 📈 前端展示要求

### 挂单记录标签页内容

1. **统计卡片区域**
   - 当前挂单数量
   - 今日触发次数
   - 总盈亏金额

2. **挂单列表表格**
   - 币种
   - 关联锚点单
   - 防线级别（第一道/第二道）
   - 基准价格
   - 触发价格
   - 触发涨幅（+4%/+10%）
   - 挂单金额
   - 杠杆倍数
   - 状态（待触发/已触发/已取消）
   - 创建时间

3. **决策日志区域**
   - 实时显示决策过程
   - 每条日志包含：币种、动作、防线级别、触发价格、执行结果、详细原因
   - 支持筛选（创建/触发/取消）
   - 支持刷新

---

## 🎨 标签页顺序调整

根据需求，标签页顺序应该调整为：

1. **统计数据** - 总览
2. **开仓记录** - 普通开仓（不含锚点单维护）
3. **补仓记录** - 普通补仓（不含锚点单维护）
4. **锚点单记录** - 所有锚点单相关
5. **挂单记录** - 为锚点单服务（新增）
6. **止盈止损** - 风控机制（新增）
7. **交易决策记录** - 所有决策汇总

---

## ⚠️ 重要提醒

1. **挂单系统仅为锚点单服务**
2. **所有规则冲突必须先由用户确认**
3. **决策日志必须记录完整的判断过程**
4. **触发后必须立即平掉95%**
5. **需要与锚点单系统紧密配合**

---

## 📞 待确认问题清单

请用户确认以下问题（按优先级排序）：

### 高优先级
1. ✅ 挂单基准价格如何确定？（冲突1）
2. ✅ 挂单创建时机是什么时候？（冲突2）
3. ✅ 挂单金额如何计算？（冲突4）
4. ✅ 挂单触发后锚点单如何处理？（冲突5）

### 中优先级
5. ✅ 锚点单补仓后挂单如何更新？（冲突3）
6. ✅ 挂单取消条件是什么？（冲突7）

### 低优先级
7. ✅ 是否需要超过两个挂单？（冲突6，从需求看应该是固定两个）

---

**请用户逐一确认以上问题，确认完成后开始实施！** 🚀
