# 信号系统完整说明

## 📋 您的三个问题及答案

### ❓ 问题1: "双重逃顶信号" 是指具体的币种吗？

**✅ 答案：是的**

- **双重逃顶信号** 是指 **具体的某个或某些币种** 同时触碰到 **压力线1 + 压力线2**
- 例如：LDOUSDT 同时触碰压力1和压力2，这就是一个双重逃顶信号
- 最新日志显示：
  - `15:34:47` - 检测到3个币种的双重逃顶信号
  - `15:44:29` - 检测到1个币种的双重逃顶信号

### ❓ 问题2: "逃顶信号" 是指压力线1 + 压力线2 >= 8 吗？

**❌ 答案：不完全正确**

**正确理解：**
- **普通逃顶信号** = 触碰压力1的币种数量 >= 8 **或** 触碰压力2的币种数量 >= 8
- **不是** 压力1和压力2的总和 >= 8

**举例说明：**
```
场景A（触发普通逃顶）:
- 压力1: 10个币种
- 压力2: 0个币种
→ 触发普通逃顶信号 ✅

场景B（不触发）:
- 压力1: 5个币种
- 压力2: 4个币种
- 总和: 9个币种
→ 不触发普通逃顶信号 ❌（因为单独的压力1和压力2都 < 8）

场景C（触发）:
- 压力1: 3个币种
- 压力2: 9个币种
→ 触发普通逃顶信号 ✅（压力2达到8个）
```

### ❓ 问题3: 这是两套独立的冷却机制吗？

**✅ 答案：是的，完全独立**

系统实现了 **4套完全独立的冷却机制**：

| 信号类型 | 触发条件 | 冷却时间 | 独立计时器 |
|---------|---------|---------|-----------|
| 1️⃣ **普通抄底信号** (buy) | 触碰支撑1或支撑2的币种 >= 8 | 300秒 | `last_buy_signal_time` |
| 2️⃣ **普通逃顶信号** (sell) | 触碰压力1或压力2的币种 >= 8 | 300秒 | `last_sell_signal_time` |
| 3️⃣ **双重抄底信号** (double_buy) | 单个币种同时触碰支撑1+2 (≥1个) | 300秒 | `last_double_buy_signal_time` |
| 4️⃣ **双重逃顶信号** (double_sell) | 单个币种同时触碰压力1+2 (≥1个) | 300秒 | `last_double_sell_signal_time` |

**独立性证明：**
- 如果 `15:30:00` 发送了双重逃顶信号，冷却到 `15:35:00`
- 在 `15:32:00` 仍然可以发送普通逃顶信号（如果满足条件）
- 两者互不影响

---

## 🔍 当前系统状态分析

### 为什么您看到的 "逃顶信号" 没有发送TG通知？

根据最新数据分析：

**1. 快照数据显示：**
```
⏰ 时间: 2025-12-22 15:45:29
   场景3 (触碰压力1): 1个币种
   场景4 (触碰压力2): 1个币种
```

**2. 触发情况：**
- ✅ **双重逃顶信号已触发**：1个币种同时触碰压力1+2
- ❌ **普通逃顶信号未触发**：压力1只有1个币种 < 8，压力2只有1个币种 < 8

**3. TG推送情况：**
- ✅ 双重逃顶信号已发送（但在冷却期内的后续信号被跳过）
- ❌ 普通逃顶信号未发送（因为不满足 >= 8个币种的条件）

---

## 📊 系统配置

### telegram_config.json 配置：
```json
{
  "signal_types": {
    "double_sell": {
      "enabled": true,
      "name": "双重逃顶信号",
      "min_coins": 1,
      "cooldown_seconds": 300
    },
    "sell": {
      "enabled": true,
      "name": "逃顶信号",
      "min_coins": 8,
      "cooldown_seconds": 300
    },
    "double_buy": {
      "enabled": true,
      "name": "双重抄底信号",
      "min_coins": 1,
      "cooldown_seconds": 300
    },
    "buy": {
      "enabled": true,
      "name": "抄底信号",
      "min_coins": 8,
      "cooldown_seconds": 300
    }
  }
}
```

---

## 🎯 核心逻辑总结

### 数据库表结构（support_resistance_snapshots）：
- `scenario_3_count`: 触碰压力1的币种数量
- `scenario_4_count`: 触碰压力2的币种数量
- `scenario_3_coins`: 触碰压力1的币种列表
- `scenario_4_coins`: 触碰压力2的币种列表

### 信号检测逻辑：

**双重逃顶（double_sell）：**
```python
if (alert_scenario_3 == 1 and alert_scenario_4 == 1):
    # 检测双重逃顶币种（同时触碰压力1+2）
    if len(double_sell_coins) >= 1:  # min_coins = 1
        发送通知
```

**普通逃顶（sell）：**
```python
if (scenario_3_count >= 8 or scenario_4_count >= 8):
    # 压力1或压力2任一达到8个币种
    if not in_cooldown:
        发送通知
```

---

## 📝 日志解读

### 您看到的日志：
```
[2025-12-22 15:34:47] 🔴🔴 检测到双重逃顶信号（压力1+2）: 3个币种
[2025-12-22 15:35:29] ⏳ 双重逃顶信号冷却中 (还需等待 258秒)
[2025-12-22 15:44:29] 🔴🔴 检测到双重逃顶信号（压力1+2）: 1个币种
```

**解释：**
- ✅ `15:34:47` 发送了双重逃顶信号（3个币种）
- ⏳ `15:35:29` 在冷却期内，跳过推送
- ⏳ 冷却期约5分钟（300秒）
- ✅ `15:44:29` 冷却结束，再次发送双重逃顶信号（1个币种）

### 为什么没有看到 "普通逃顶" 日志？
因为从 `15:41:28` 到 `15:45:29` 的快照数据显示：
- 压力1最多只有1个币种（< 8）
- 压力2最多只有1个币种（< 8）
- **不满足普通逃顶的触发条件**

---

## ✅ 结论

1. **双重逃顶信号** = 具体币种（如 LDOUSDT）同时触碰压力1+2 ✅
2. **普通逃顶信号** = 压力1达到8个币种 **或** 压力2达到8个币种（不是总和）✅
3. **独立冷却机制** = 4套完全独立的冷却计时器 ✅

**当前情况：**
- 您在网页 https://5000-ilsitop6yown44mau7vd7-c07dda5e.sandbox.novita.ai/support-resistance 上看到的信号
- 如果显示有币种同时触碰压力1+2 → 触发 **双重逃顶信号**（TG已推送）
- 如果显示触碰压力1或压力2的币种总数很多，但单独的压力1 < 8 且压力2 < 8 → **不触发普通逃顶信号**

**建议：**
如果希望调整触发阈值，可以修改 `telegram_config.json` 中的 `min_coins` 参数：
- 降低 `sell.min_coins` 从 8 到更小的值（如5）
- 然后执行 `pm2 restart telegram-notifier`

---

## 📚 相关文档
- `/home/user/webapp/TG_NOTIFICATION_SYSTEM_EXPLAINED.md`
- `/home/user/webapp/telegram_config.json`
- `/home/user/webapp/telegram_notifier.py`

**生成时间**: 2025-12-22 15:46:00
