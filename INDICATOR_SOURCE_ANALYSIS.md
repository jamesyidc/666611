# 技术指标数据源分析报告

## 🎯 核心发现

### ❌ 选项2不可行：OKEx 不提供技术指标 WebSocket

经过深入调查，我发现：

**没有任何加密货币交易所直接提供技术指标的 API 或 WebSocket！**

包括：
- ❌ OKEx (欧易)
- ❌ Binance (币安)
- ❌ Bybit
- ❌ Coinbase
- ❌ Kraken

### 🔍 交易所只提供原始数据

所有交易所的 API 只提供：
- ✅ K线数据 (OHLCV)
- ✅ 实时价格 (ticker)
- ✅ 深度数据 (order book)
- ✅ 成交记录 (trades)

### 💡 技术指标都是"计算"出来的

无论是哪个平台，技术指标都是基于 K线数据计算的：

| 平台 | 计算位置 | 数据来源 |
|------|----------|----------|
| OKEx 网页版 | 🖥️ 浏览器前端 JavaScript | OKEx K线 API |
| OKEx APP | 📱 手机客户端 | OKEx K线 API |
| TradingView | ☁️ TradingView 服务器 | 多个交易所 K线 |
| 我的系统 | 🔢 Python 后端 (TA-Lib) | OKEx K线 API |

---

## 🔬 问题分析：为什么 SAR 计数不一致？

### 您的观察

**欧易实盘**（图2）: UNI 5m = **空头01** ✅  
**我的系统**（图1）: UNI 5m = **空头06** ❌

### 差异原因

#### 1. 数据采集时间点不同
```
欧易前端: 实时 WebSocket 推送，立即更新
我的系统: REST API 定期采集（例如每5分钟）
→ 可能采集到的是不同批次的K线数据
```

#### 2. 历史K线范围不同
```
SAR 计算需要历史数据来确定"转折点"

欧易可能用: 最近 50根 或 页面可见的K线
我的系统用: 最近 100根K线

→ 如果取的范围不同，SAR 转折点就会被识别在不同位置
```

#### 3. SAR 周期计数的起始点
```
"空头01" 意味着: 刚刚从多头转为空头，这是第1根空头K线
"空头06" 意味着: 已经持续空头6根K线了

问题在于: 如何定义"转折点"？
- 欧易: 可能基于实时最新K线判断转折
- 我的系统: 基于历史100根K线计算，识别的转折点在更早的位置
```

---

## 📊 实时验证：TradingView 数据

刚才测试了 TradingView 对 UNI 5m 的数据：

```
时间: 2025-12-11 14:00
价格: $5.364
RSI(14): 49.76
SAR: 5.3548 [多头]
```

**与欧易实盘对比**：
- 欧易显示: **空头01**
- TradingView: **多头** (SAR < 价格)

这说明：
1. ⚠️ 不同平台的 SAR 判断可能不同
2. ⚠️ TradingView 不提供 "周期计数"，只告诉你多头/空头

---

## 🎯 根本问题

### 您的要求："要采集，不能计算"

**残酷的真相**：
> 🚫 **不存在"直接采集技术指标"的数据源**
> 
> 所有的技术指标（RSI, SAR, BOLL）都是**计算**出来的！

即使是欧易官网显示的指标，也是：
1. 后端/前端从 OKEx API 获取 K线数据
2. 使用 JavaScript 或其他语言计算 RSI、SAR、BOLL
3. 在图表上展示

---

## 💡 可行的解决方案

### 方案A：TradingView API（有限的"采集"）

**优势**：
- ✅ 相对"权威"（TradingView 是专业图表平台）
- ✅ 不需要自己实现计算逻辑
- ✅ 支持多个交易所

**劣势**：
- ❌ 速率限制（HTTP 429 错误）
- ❌ **不提供 SAR 周期计数**（只有多头/空头状态）
- ❌ 仍然是 TradingView "计算"好的，不是交易所原始数据

**改进方案**：
```python
# 添加速率限制处理
1. 每个请求间隔 3-5 秒
2. 遇到 429 自动重试（指数退避）
3. 分批采集（每批5个币种）
4. 使用缓存减少重复请求
```

### 方案B：完全模拟欧易前端（推荐）

既然欧易前端也是"计算"的，我们可以：

**步骤**：
1. 使用 OKEx WebSocket 获取实时 K线
2. 使用与欧易**完全相同**的算法参数：
   ```python
   # 需要逆向工程欧易的参数
   SAR: acceleration=?, maximum=?
   历史K线范围: ? 根
   ```
3. 实现与欧易相同的 SAR 周期计数逻辑

**优势**：
- ✅ 实时性最强（WebSocket）
- ✅ 可以匹配欧易的显示
- ✅ 无速率限制

**劣势**：
- ❌ 需要逆向工程欧易的参数
- ❌ 仍然是"计算"（因为没有"采集"的选项）

### 方案C：妥协方案

接受"计算是必须的"这个现实，专注于：
1. ✅ 确保计算**实时性**（WebSocket）
2. ✅ 确保计算**准确性**（标准算法）
3. ✅ 提供**多种数据源**对比（TradingView + 自己计算）

---

## 🔍 我的建议

根据您的需求"要采集不要计算"，以及技术现实，我建议：

### 短期方案：修复 TradingView API
```
1. 实现速率限制处理
2. 分批采集27个币种
3. 每5分钟更新一次
4. 明确告知用户：这是 TradingView 计算的结果
```

### 长期方案：实现 WebSocket 实时计算
```
1. OKEx WebSocket 订阅 5m/1h K线
2. 每根K线闭合时重新计算指标
3. 尽可能匹配欧易官网的显示
4. 提供"数据源"选择（TradingView vs 自己计算）
```

---

## ❓ 关键问题

**请您决定**：

1. **接受 TradingView 方案**（相对权威，但没有周期计数）
   - 优点：不需要自己计算，相对"采集"
   - 缺点：速率限制，没有 SAR 周期计数

2. **实现 WebSocket + 实时计算**（完全自主）
   - 优点：实时性强，可以实现周期计数
   - 缺点：需要自己计算（与您的原则冲突）

3. **混合方案**（双数据源对比）
   - 同时提供 TradingView 和 TA-Lib 的结果
   - 用户可以对比差异
   - 更透明

---

**您希望采用哪个方案？**
