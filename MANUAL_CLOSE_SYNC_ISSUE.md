# 手动平仓数据同步问题说明

**问题时间**: 2025-12-28  
**状态**: ⚠️ 已发现，待修复

---

## 一、问题现象

用户点击"平仓保留1U"按钮后：
1. **立即刷新**: 持仓数量正确更新（例如：60.0 → 7.3189）
2. **15秒后**: 持仓数量又恢复原样（7.3189 → 60.0）
3. **反复出现**: 每次手动平仓后都会被覆盖

---

## 二、问题原因

### 根本原因

系统中有**两个数据源**在争夺`position_opens`表的控制权：

1. **手动平仓API** (`trading_api.py`)
   - 用户点击按钮 → 只更新数据库
   - UPDATE `position_opens` SET `open_size` = 7.3189

2. **持仓同步守护进程** (`sync_positions_fast.py`)
   - 每15秒从OKX API同步实际持仓
   - UPDATE `position_opens` SET `open_size` = 60.0（OKX实际持仓）

### 执行流程

```
时间轴:
00:00  用户点击"平仓保留1U"
00:01  ✅ 数据库更新: 60.0 → 7.3189
00:01  ✅ 用户看到: 7.3189（正确）
00:15  ❌ 同步程序: 从OKX获取实际持仓 = 60.0
00:15  ❌ 数据库覆盖: 7.3189 → 60.0
00:16  ❌ 用户刷新: 又看到 60.0
```

### 相关进程

| 进程名 | PID | 功能 | 同步间隔 | 问题 |
|--------|-----|------|---------|------|
| sync_positions_fast.py | 526603 | 同步OKX持仓 | 15秒 | ❌ 覆盖手动操作 |
| position_system_collector.py | 574504 | 采集持仓数据 | 变化 | ❌ 可能覆盖 |
| anchor_system.py | 359659 | 锚点单系统 | 监控 | ✅ 不覆盖 |
| anchor_opener_daemon.py | 565710 | 锚点单开仓 | 30秒 | ✅ 不覆盖 |

---

## 三、数据验证

### FIL持仓示例

#### 手动平仓前
```
ID: 40
数量: 60.0
价格: 1.3663
名义价值: 81.98 USDT
```

#### 手动平仓后（立即）
```
ID: 40
数量: 7.3189  ✅ 正确
价格: 1.3663
名义价值: 10.00 USDT  ✅ 保留1U保证金
更新时间: 19:49:06
```

#### 15秒后（被同步覆盖）
```
ID: 40
数量: 60.0  ❌ 恢复原值
价格: 1.3663
名义价值: 81.98 USDT  ❌ 又回到原值
更新时间: 19:49:21  ❌ 被sync_positions_fast更新
```

### 平仓记录验证

```sql
SELECT * FROM position_closes WHERE inst_id = 'FIL-USDT-SWAP' ORDER BY created_at DESC;

-- 结果：3条平仓记录
ID  close_size  close_reason                          created_at
6   52.68       手动平仓保留1U保证金（10.00U名义）    2025-12-28 19:49:06
3   52.68       手动平仓保留1U保证金（10.00U名义）    2025-12-28 19:43:31
1   52.68       手动平仓保留1U保证金（10.00U名义）    2025-12-28 19:35:25
```

✅ 平仓记录正确插入  
❌ 但持仓数量被覆盖

---

## 四、解决方案

### 方案1：集成OKX API真实平仓（推荐）

#### 优点
- ✅ 真正平仓，OKX实际持仓减少
- ✅ 同步程序获取的就是正确的持仓
- ✅ 数据一致性最好

#### 缺点
- ❌ 需要OKX API权限
- ❌ 需要处理API错误
- ❌ 开发成本较高

#### 实现步骤
1. 在`trading_api.py`中添加OKX平仓函数
2. 手动平仓时调用OKX API
3. API成功后再更新数据库
4. 15秒后同步程序获取的就是正确持仓

### 方案2：添加"手动操作标记"（临时方案）

#### 优点
- ✅ 无需OKX API
- ✅ 开发简单快速
- ✅ 可以立即部署

#### 缺点
- ❌ 数据库与OKX实际持仓不一致
- ❌ 需要定期清理标记
- ❌ 可能被同步程序忽略

#### 实现步骤
1. `position_opens`表添加`manual_close_flag`字段
2. 手动平仓时设置flag=1
3. 同步程序跳过flag=1的记录
4. 定期或手动清理flag

### 方案3：临时停止同步程序（不推荐）

#### 优点
- ✅ 立即生效
- ✅ 手动操作不会被覆盖

#### 缺点
- ❌ 无法获取OKX实际持仓变化
- ❌ 数据不同步
- ❌ 影响系统功能

---

## 五、推荐修复流程

### 立即措施（临时）
1. **添加说明**：在UI上提示用户"此操作仅更新数据库，不会真实平仓"
2. **显示提示**：平仓成功后提示"15秒后数据会被同步覆盖"

### 短期修复（1-2天）
1. 实现方案2：添加手动操作标记
2. 修改`sync_positions.py`跳过标记的记录
3. 测试验证

### 长期修复（1周）
1. 实现方案1：集成OKX API
2. 调用真实平仓接口
3. 完整测试

---

## 六、临时解决方法

### 用户侧
1. **立即查看**：平仓后立即刷新查看，此时数据是对的
2. **等待同步**：15秒后会恢复原值（OKX实际持仓）
3. **真实平仓**：需要登录OKX App手动平仓

### 开发侧
1. **停止同步**（临时）:
   ```bash
   pm2 stop sync_positions_fast
   pm2 stop position-system-collector
   ```
   
2. **重启同步**:
   ```bash
   pm2 restart sync_positions_fast
   pm2 restart position-system-collector
   ```

---

## 七、相关文件

| 文件 | 功能 | 修改需求 |
|------|------|---------|
| trading_api.py | 手动平仓API | ✅ 需添加OKX API调用 |
| sync_positions.py | 持仓同步逻辑 | ✅ 需跳过手动操作标记 |
| sync_positions_fast.py | 快速同步守护进程 | ⚠️ 可能需调整 |
| position_system_collector.py | 持仓数据采集 | ⚠️ 可能需调整 |

---

## 八、Git记录

此问题的相关commit：
- `6333453`: fix(display): 修复锚点单盈亏率显示N/A问题
- `7f17774`: fix(api): 修正价格数据源
- `d6a4c01`: fix(anchor): 修复new_size变量未定义错误

---

## 九、总结

### 问题核心
- ❌ 手动平仓只更新数据库
- ❌ 同步程序每15秒覆盖数据库
- ❌ 用户看到的数据被反复覆盖

### 根本解决
- ✅ 集成OKX API真实平仓
- ✅ 让数据库与OKX保持一致
- ✅ 同步程序获取的就是正确数据

---

**创建时间**: 2025-12-28  
**优先级**: 🔴 高  
**预计修复时间**: 1-2天（临时方案）/ 1周（完整方案）
